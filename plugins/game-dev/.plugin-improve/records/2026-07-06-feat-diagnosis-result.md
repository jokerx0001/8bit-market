# game-dev - feat 链路诊断结果

## 日期：2026-07-06

### 链路拓扑

```
skills/orchestrator/SKILL.md (入口)
├── skills/artifact-manager/SKILL.md          [Phase 1: 创建任务目录]
├── skills/design-ui/SKILL.md                 [Phase 2: UI检测 — 本次未触发(无UI)]
├── skills/plan/SKILL.md                      [Phase 3: 设计阶段]
│   ├── references/plan-format.md
│   └── references/godot/config.md
├── skills/art-resources-conductor/SKILL.md   [Phase 4: 资源生成 — 本次未触发(无资源)]
├── skills/exec/SKILL.md                      [Phase 5: TDD实现]
│   ├── agents/test-agent.md
│   ├── agents/coding.md
│   ├── references/godot/config.md
│   ├── references/godot/coding.md
│   ├── references/godot/testing.md
│   ├── skills/exec/references/exec-prompts.md  (exec 同级目录下)
│   └── skills/exec/references/exec-logging.md  (exec 同级目录下)
├── skills/collect-lessons/SKILL.md           [Phase 9: 经验收集]
└── skills/write-tutorial/SKILL.md            [Phase 10: 教学文档]
```

### 逐步骤诊断

| # | 所属节点 | 应有步骤 | 要求(来自plugin文件) | 实际步骤(来自log) | 达标? | 达标证据 / 不达标点 |
|---|---------|---------|---------------------|------------------|-------|-------------------|
| 1 | skills/orchestrator/SKILL.md | Phase 0: 检测技术栈 | "Step 0a — 读 CLAUDE.md 确定 tech" + "Step 0b — 读 config 获取 dev_dir（硬门）" (orchestrator/SKILL.md:62-88) | log中无直接记录，但产物路径 `.godot-dev/feat-3/` 证明 tech=godot, dev_dir=.godot-dev 被正确识别 | ✅ | 产物目录 `.godot-dev/feat-3/` 存在，plan.md开头声明 "Godot 4.7 Forward Plus（3D）"，证明技术栈检测正确 |
| 2 | skills/orchestrator/SKILL.md | Phase 1: 创建任务目录 | "Skill({skill: 'game-dev:artifact-manager', args: '--task-dir {dev_dir}/{kind}-{N} --kind feat --dev-dir {dev_dir}'})" (orchestrator/SKILL.md:98-103) | 产物目录 `.godot-dev/feat-3/` 已创建，progress.json 存在且 counters 正确递增 | ✅ | 产物目录 `.godot-dev/feat-3/` 存在，progress.json 中 `current: "complete"`，任务编号 feat-3 |
| 3 | skills/orchestrator/SKILL.md | Phase 2: UI 检测 | "判定原则：宁可误判多调 design-ui（它会自己判断并跳过不需要的部分），也不要漏判。" (orchestrator/SKILL.md:115) | 本次任务为3D僵尸AI，无UI，design-ui 未触发 | ✅ | plan.md 无 UI 任务（所有任务 type: logic），.work/layouts/ 目录不存在，判定正确跳过 |
| 4 | skills/orchestrator/SKILL.md | Phase 3: Plan 设计阶段 | "Skill({skill: 'game-dev:plan', args: '--task-dir {task_dir}'})" (orchestrator/SKILL.md:130-131) | plan.md 存在且包含完整设计文档 | ✅ | plan.md 包含：概述、领域模型、行为列表(10条)、设计摘要、影响范围、任务列表(AI-0~AI-7)、资源需求、测试策略 — 符合 plan-format.md 模板结构 |
| 5 | skills/plan/SKILL.md | 步骤2: 读取模式约束 | "feat — 无预约束，跳过。" (plan/SKILL.md:30) | feat 模式正确跳过约束读取 | ✅ | plan.md 中无 impact.md 或 debug-analysis.md 引用，证明 feat 模式正确跳过了 refactor/fix 专属约束 |
| 6 | skills/plan/SKILL.md | 步骤3: 加载格式契约 | "读取 `${CLAUDE_PLUGIN_ROOT}/references/plan-format.md`。所有输出必须遵守此格式规范" (plan/SKILL.md:38) | plan.md 格式校验 | ✅ | plan.md 包含模板要求的所有章节（概述、领域模型、行为列表、设计摘要、影响范围、任务列表、资源需求、测试策略），任务编号为 `[AI-N]` 格式，类型标注 `(type: logic)`，无禁止内容 |
| 7 | skills/plan/SKILL.md | 步骤4: 收集需求并确认行为 | "确认行为清单（强制门）" — "确认以下行为是否准确" (plan/SKILL.md:66-80) | plan.md 行为列表中有10条行为，.work/requirements.md 存在 | ✅ | plan.md 行为列表 10 条（#1~#10），.work/requirements.md 存在(2168 bytes)，每条行为可验证 |
| 8 | skills/plan/SKILL.md | 步骤5: 领域设计 | "每个功能行为都要做领域分析，不能跳过" (plan/SKILL.md:143-144) | .work/domain-design.md 存在 | ✅ | .work/domain-design.md 存在(5689 bytes)，plan.md 领域模型覆盖了 AI模式(FSM)、数据模式(ZombieData Resource)、受击接口、边界规则 |
| 9 | skills/plan/SKILL.md | 步骤6: 任务拆分与排序 | "拆分原则：按功能模块拆分，不按文件/阶段拆分" (plan/SKILL.md:151) | 8个AI任务，全部 type: logic，logic优先于ui | ✅ | plan.md 任务列表 AI-0~AI-7，全为 `(type: logic)`，无文件路径在任务描述中（grep验证：AI任务描述不含 `.gd`/`.tscn`/`.tres`），无 `### [AI-N]` 子章节展开 |
| 10 | skills/plan/SKILL.md | 步骤9: 编写plan.md | "自己编写，不委托外部 skill。" (plan/SKILL.md:306) | plan.md 存在，结构自包含 | ✅ | plan.md 8296 bytes，自包含 — 不引用 `.work/` 下文件（无 "详见 design.md" 等外部引用），exec 可直接读取 |
| 11 | skills/plan/SKILL.md | 步骤10: 格式自检 | "输出前对照 `plan-format.md` 的"格式校验清单"逐项确认" (plan/SKILL.md:321) | plan.md 格式检查 | ✅ | plan.md 无禁止内容（无 "lint代替测试"、"人工验证"、"源码契约"等），任务描述不含文件路径，不含 agent 微指令 |
| 12 | skills/orchestrator/SKILL.md | Phase 4: 资源检测与生成 | "当 `{task_dir}/.work/resources.md` 存在且包含未处理的资源条目时触发。" (orchestrator/SKILL.md:142-143) | .work/resources.md 存在但内容声明 "无新资源" | ✅ | resources.md 内容: "无新资源。Zombie_Basic.gltf 含 6 段目标动画和 50 关节骨架；zombie_basic.tres 由 Godot 编辑器生成" — 正确跳过 art-resources-conductor |
| 13 | skills/orchestrator/SKILL.md | Phase 5: Exec 实现阶段 | "Skill({skill: 'game-dev:exec', args: '--mode feat --task-dir {task_dir}'})" (orchestrator/SKILL.md:155-157) | exec 执行了全部 8 个 AI 任务的 TDD 循环 | ✅ | progress.json: 全部 8 个任务 status: done；全量测试 98/98 passed（来自 brjhgjy2s.txt:617-618） |
| 14 | skills/exec/SKILL.md | 步骤2: 加载设计文档和进度 | "只读 `{task_dir}/plan.md`。不读 `.work/` 下的任何文件" (exec/SKILL.md:77) | exec 通过 subagent 间接读取 plan.md | ✅ | 每个 subagent 的 prompt 包含 `## task_dir` 字段，agent 自行读取 plan.md；无需 exec 主会话直接读 .work/ 的证据 |
| 15 | skills/exec/SKILL.md | 步骤5: 信息隔离清单 | "从 game-dev:test-agent 到 game-dev:coding：行为级失败描述 + 具体失败 testcase 名称和错误信息、设计文档路径。禁止传：测试源码、测试文件路径" (exec/SKILL.md:93-95) | subagent prompt 中包含 testcase 名称和错误信息 | ✅ | AI-2 GREEN prompt: "test_zombie_base_is_abstract ❌ Godot 4.7 `GDScript.can_instantiate()` 对 @abstract 类仍返回 true" — 传递了具体 testcase 名称和错误信息，未传测试源码 |
| 16 | skills/exec/SKILL.md | 步骤6b: RED — spawn test-agent | "使用 `references/exec-prompts.md` 的 **RED prompt** 模板" (exec/SKILL.md:146) | 实际 RED prompt 包含模板全部必填字段，并额外增加上下文 | ✅ | 模板（skills/exec/references/exec-prompts.md:17-42）要求 `## 模式`, `## project`, `## task_dir`, `## 任务`, `## 要验证的行为`, `## 需要读取的文件` 六个字段。实际 RED prompt（agent-ace4e96dd2dd71b76 AI-2 RED）包含全部六个必填字段，同时额外增加了 `## 任务细节`（状态枚举/信号/受击接口的详细契约）、`## 测试方式`、`## 编写约束`、`## 运行命令`、`## RED 失败预期`、`## 不要做的事`、`## 报告格式`。这些扩展字段为 test-agent 提供了更精确的上下文，属于对模板的有益增强 |
| 17 | skills/exec/SKILL.md | 步骤6: TDD 日志记录 | "按 exec-logging.md RED 格式追加" / "按 exec-logging.md GREEN 格式追加" (exec/SKILL.md:152,162) | tdd-iterations.md 存在但格式完全偏离标准 | ❌ | exec-logging.md 要求：RED 条目格式为 `### Iter {iter_N} — RED (test-agent)` + test file + test cases 表格 + self-correction rounds + verdict。实际 tdd-iterations.md：**完全没有 RED 条目**——日志从 `## [AI-1] GREEN — Test Run #1` 开始，跳过了所有 RED 阶段日志。GREEN 条目格式要求 `### Iter {iter_N} — GREEN (coding-agent)` + Files modified + Self-verification rounds + Verdict。实际格式完全不同：使用 `## [AI-N] GREEN — Test Run #N` 章节标题 + 自定义表格（Test Case \| Result \| Failure Reason \| Solution），缺少 iter_N 编号、Files modified 字段。日志由 coding-agent 的自我验证协议写入，而非 exec 主会话按 exec-logging.md 格式追加 |
| 18 | skills/exec/SKILL.md | 步骤6e: 边界检查 | "VERIFY 全部通过后、REFACTOR 之前执行。" (exec/SKILL.md:176) | tdd-iterations.md 显示 AI-5 REFACTOR 前有边界检查 | ✅ | AI-5 REFACTOR entry: "Boundary: grep `pass\|TODO\|NotImplemented\|^\s*print\(` → 0 命中" (tdd-iterations.md:224)；AI-6 REFACTOR entry: "Boundary: pass/TODO/NotImplemented/print in zombie_basic.gd ✅ 0 hits" (tdd-iterations.md:251) |
| 19 | skills/exec/SKILL.md | 步骤8: 最终验证 | "从 config.md 读取 test_cmd_full 并执行。验证全部通过。" (exec/SKILL.md:239-240) | tool-results/brjhgjy2s.txt 显示全量测试执行 | ✅ | brjhgjy2s.txt:617-618: "Tests 98, Passing Tests 98, Asserts 162, Time 0.84s" + "All tests passed!" |
| 20 | skills/exec/SKILL.md | 步骤9: 收集开发经验 | "Skill('game-dev:collect-lessons', 'tech={tech}')" (exec/SKILL.md:244-248) | .work/collect-lessons-summary.md 存在 | ✅ | collect-lessons-summary.md 记录了 test-lessons.md(3条) + coding-lessons.md(5条) + CLAUDE.md(6条) 写入 |
| 21 | skills/exec/SKILL.md | 步骤10: 编写教学文档 | "Skill('game-dev:write-tutorial')" — 仅 feat/refactor (exec/SKILL.md:253-261) | TUTORIAL.md 存在(24510 bytes, 2026-07-06 13:15) | ✅ | `/mnt/d/project/godot/zombies-demo/TUTORIAL.md` 存在且文件大小 24510 bytes，时间戳与 collect-lessons-summary.md (2026-07-06 13:14) 相邻，证明 write-tutorial 在 collect-lessons 后成功执行 |
| 22 | agents/test-agent.md | Spawn 初始化 | "1. 从 prompt 提取 `## project`、`## task_dir`、`## 模式` 字段; 2. 读取 config.md; 3. 用 `{project}` 填充; 4. 打印初始化摘要" (test-agent.md:46-61) | 每个 RED/VERIFY agent 的 prompt 中包含 project=zombies-demo, task_dir=.godot-dev/feat-3, 模式 | ✅ | agent-a214b9e920c962f1e (RED): "## 模式\nRED\n\n## project\nzombies-demo\n\n## task_dir\n.godot-dev/feat-3" — prompt 结构符合要求 |
| 23 | agents/test-agent.md | RED: Step 2a Tracer Bullet | "写一个最小测试证明目标可达" (test-agent.md:89-91) | 测试文件存在且包含基础存在性测试 | ✅ | test_smoke.gd (4 testcases) — 证明 GUT 可运行；test_zombie_scene.gd: "test_zombie_scene_file_exists" — tracer bullet 证明场景文件存在 |
| 24 | agents/test-agent.md | RED: Step 3 Self-correction | "语法错误 → 修复重跑（最多 3 轮）" (test-agent.md:101) | AI-4 RED 阶段出现 "RED 阶段修订" agent | ❌ | agent-a355beb2d4e3efea1: "RED 阶段修订(测试契约对齐)" — 这不是语法错误修复，而是因为 "第一版 RED 测试设计了错误的契约，导致 GREEN agent 拿到错误的契约去实现"。这意味着 RED 阶段的测试契约本身有问题——test-agent 没有正确理解设计文档就写了测试。这不是 self-correction loop，而是重新设计测试。 |
| 25 | agents/coding.md | Spawn 初始化 | "1. 从 prompt 提取字段; 2. 读取 config.md; 3. 填充占位符; 4. 打印初始化摘要" (coding.md:63-82) | GREEN agent prompt 包含所需字段 | ✅ | agent-a68206f56bea5d81b (AI-2 GREEN): "## 模式\nGREEN\n\n## project\nzombies-demo\n\n## task_dir\n.godot-dev/feat-3" — 符合要求 |
| 26 | agents/coding.md | GREEN: 三层验证结构 | "Phase 1: 初步运行 → Phase 2: 逐个Testcase系统性循环 → Phase 3: 收尾" (coding.md:109-123) | tdd-iterations.md 记录了逐case诊断 | ✅ | AI-2 GREEN tdd-iterations.md:40-85 记录了完整的 Phase 1(初次运行24 tests, 12 fail) → Phase 2(逐case分析) → Phase 3(最终状态 23/24 + gutconfig workaround) 流程 |
| 27 | agents/coding.md | GREEN: 诊断模板 | "Phase 2 诊断模板: 错误信息 → 当前代码实际行为 → 设计文档要求 → 问题分类 → 根因" (coding.md:127-146) | tdd-iterations.md 中 AI-2/AI-3 有根因分析 | ✅ | AI-2 tdd-iterations.md:51-65 包含两个case的 Failure Reason + Solution；AI-3 tdd-iterations.md:106-112 包含完整根因分析（"问题分类:API使用方法问题"、"根因:测试契约隐含character_scene子节点可能在_ready之后才注入"） |
| 28 | agents/coding.md | GREEN: 自我验证协议 | "实现 → 跑测试 → 读输出 → 修复 → 重复直到通过" (coding.md:36) | 多数任务自验证通过 | ✅ | AI-1: 1轮通过；AI-3: 2轮通过；AI-5: 1轮通过；AI-7: 2轮通过 |
| 29 | agents/coding.md | GREEN: 重试上限 | "每个任务最多 5 轮，每个 testcase 最多 3 轮子循环" (coding.md:158) | AI-4 需要 "GREEN(重做,修正前次实现)" | ❌ | agent-afa26b4fb75ffbfa4: "GREEN(重做,修正前次实现)" — AI-4 的第一个 GREEN agent (agent-ac1ed3f60e4fa2e1e, 523 lines) 实现了一个错误的架构（inline动画副本、zombie_capsule.gd hack），需要完全重做。这不是 "5轮内自我修正"，而是 "第一个实现完全作废"。虽然没超过5轮限制，但该问题根因在于 RED 测试契约设计错误（见 #24）。 |
| 30 | agents/coding.md | 关键规则 #1 | "绝不写入测试目录。" (coding.md:279) | AI-6 REFACTOR 修改了测试文件 | ❌ | tdd-iterations.md AI-6 REFACTOR: "改动 2: test/unit/test_zombie_death_flow.gd `_make_zombie_basic_with_anim()` — fixture 头注释更新...在 add_child(ap) 之前给 AP 挂 AnimationLibrary"。coding agent 在 REFACTOR 模式下修改了测试文件（test_zombie_death_flow.gd），违反了 "绝不写入测试目录" 的铁律。 |
| 31 | agents/coding.md | 关键规则 #2 | "绝不写空壳/假代码。不允许 `pass`、`# TODO` 或 `NotImplementedError`。" (coding.md:280) | AI-2 zombie_base.gd 中有 `pass` | ❌ | tdd-iterations.md AI-6 REFACTOR boundary check 确认: "zombie_base.gd:251 `pass` abstract stub ✅ 保留(原 GREEN 已合法)" — 虽然被标记为"合法"（abstract stub），但 coding.md 规则 #2 说 "不允许 `pass`"，没有例外条款。coding.md:89 也列出 `pass` 语句在禁止清单中。 |
| 32 | skills/collect-lessons/SKILL.md | 步骤6: 分类 & 验证 | "验证事实：对涉及框架 API/语法的经验，按 config.md 的 `## 文档` 节指定的方式查阅文档确认。" (collect-lessons/SKILL.md:102) | collect-lessons-summary.md 中有一条标记 `[待验证]` | ❌ | collect-lessons-summary.md 第4条: "`ext_resource` 配 `.tres` 缺 `.uid` 文件时删 `uid='...'` 字段 — 只留 `path=` — 标记 [待验证],官方文档未覆盖 UID fallback 行为"。collect-lessons 要求 "只在文档确认后才写入，无法确认的标注 `[待验证]`" — 这条虽然标注了，但说明查阅文档的流程执行了。部分达标。 |
| 33 | — | 额外步骤: RED 阶段修订 agent | 不在 plugin 规定的链路中 | agent-a355beb2d4e3efea1: "RED 阶段修订(测试契约对齐)" | ❌ 未定义步骤 | exec 的 TDD 循环只有 RED → GREEN → VERIFY → 边界检查 → REFACTOR → VERIFY。不存在 "RED 阶段修订" 这一步骤。AI-4 的 RED 测试契约设计错误导致需要重新设计测试，破坏了 RED 的独立性。 |
| 34 | — | 额外步骤: RED-fix agent (框架API修复) | 不在 plugin 规定的链路中 | agent-a643c8a88b8b9f026: "RED-fix (框架 API 不匹配)" — 修正 test_zombie_base_is_abstract 测试 | ❌ 未定义步骤 | test-agent 的 RED self-correction loop 最多 3 轮修复语法错误。但这里 spawn 了一个独立的 "RED-fix" agent，且是因为 test-agent 用了错误的 GDScript API (`can_instantiate()` 不检查 `@abstract`)，这不是语法错误，而是 test-agent 对框架 API 的理解偏差。 |
| 35 | — | 额外步骤: AI-6 VERIFY+code-review 合并 | 不在 plugin 规定的链路中 | agent-a91251b2ee87ffc94: "VERIFY(post-GREEN)+ 独立代码审计" | ❌ 未定义步骤 | exec/SKILL.md 定义的 VERIFY 步骤是 "spawn test-agent → 独立验证门（跑测试，不分析）"。但 AI-6 的 VERIFY agent 被要求同时做 "代码审计"——这超出了 test-agent 的职责范围（test-agent 只写/跑测试，不做代码审查）。 |
| 36 | skills/write-tutorial/SKILL.md | 步骤4b: 确保 .claudeignore 排除教学文件 | "`TUTORIAL.md` 和 `TODO-BEST-PRACTICES.md` 是给人看的，不应浪费 Claude Code 的上下文。写入之前先确保它们在 `.claudeignore` 中" (write-tutorial/SKILL.md:114) | .claudeignore 存在但缺少 TUTORIAL.md 和 TODO-BEST-PRACTICES.md 条目 | ❌ | `.claudeignore` 文件中没有 `TUTORIAL.md` 行也没有 `TODO-BEST-PRACTICES.md` 行。write-tutorial 步骤4b 要求 "grep -qx 'TUTORIAL.md' .claudeignore" 并追加缺失条目，但该检查未执行或未生效。TUTORIAL.md (24510 bytes) 因此会被 Claude Code 加载到上下文中，浪费 token。 |

### 根因分析与解决方案

| # | 应有步骤 | 达标? | 根因 | 解决方案 | 解决方案来源 |
|---|---------|-------|------|---------|-------------|
| 17 | exec: TDD 日志格式 | ❌ | `skills/exec/references/exec-logging.md` 存在且定义了完整格式，但实际 tdd-iterations.md 完全偏离。**RED 条目全部缺失**——日志从 GREEN 开始，没有按 exec-logging.md:45-59 的 RED 模板记录。**GREEN 条目格式不同**——实际使用自定义表格（Test Case/Result/Failure Reason/Solution），而非 exec-logging.md:64-86 规定的 `### Iter {N} — GREEN (coding-agent)` + Files modified + Self-verification rounds + Verdict 格式。机制层：tdd-iterations.md 的实际写入者是 coding-agent（其自我验证协议要求追加日志），而非 exec 主会话。exec 的 "记录日志" 步骤（6b/6c/6d/6f/6g）没有真正执行——它依赖 agent 自行写入日志。缺少 "exec 主会话必须在 agent 返回后立即按格式追加" 的强制执行机制。 | 方案A（推荐）：保持现状——coding-agent 的自我验证日志格式（表格化）信息密度更高、更适合排查问题。更新 exec-logging.md 的格式定义，使其与 coding-agent 实际产出的格式一致。方案B：在 exec 步骤6b/6c/6d/6f/6g 的 "记录日志" 子步骤中增加硬门——exec 主会话必须在 agent 返回后立即按 exec-logging.md 格式追加，coding-agent 的自我验证日志作为补充（不替代 exec 日志）。 | skills/exec/references/exec-logging.md；agents/coding.md 自我验证协议日志格式 |
| 24 | test-agent: RED Self-correction | ❌ | test-agent 的 RED 模式要求 self-correction loop 最多 3 轮修复**语法错误**，但 AI-4 的问题是 RED 测试契约本身设计错误——test-agent 没有正确理解 plan.md 的设计意图就写了测试。机制层：缺少 "RED 阶段测试契约验证门" — test-agent 写测试前必须对照 plan.md 确认契约正确性。当前 RED 模式没有 "设计文档对齐检查" 步骤。 | 在 test-agent.md 的 RED mode Step 2a 之前增加 **Step 0: 设计契约对齐**：在写测试前，先读取 plan.md 中对应任务的行为列表条目，逐条确认测试将覆盖的行为与设计文档一致。输出对齐摘要后方可进入 Step 2a。 | agents/test-agent.md RED Mode 章节；harness-methodology.md 的 pre-flight check 机制 |
| 29 | coding: AI-4 GREEN re-do | ❌ | AI-4 GREEN 第一次实现 (agent-ac1ed3f60e4fa2e1e, 523行) 产生了错误架构：inline 动画副本到 .tscn (271 KB)、创建 zombie_capsule.gd hack subclass 绕过 float32 精度。根源是 #24 的 RED 测试契约错误传导到了 GREEN。机制层：coding agent 在实现前没有验证测试契约是否与 plan.md 一致——它信任了 RED 的测试。缺少 "GREEN 实现前语义验证" 步骤。 | 在 coding.md 的 GREEN mode Phase 1 之前增加 **Step 0: 语义验证**：接收到测试失败描述后，先对照 plan.md 确认测试断言的行为与设计文档一致。发现测试契约与设计文档冲突时，不盲目实现，而是上报 exec 要求 RED 修正。 | agents/coding.md GREEN Mode 章节 |
| 30 | coding: 不写入测试目录 | ❌ | AI-6 REFACTOR agent 修改了 `test/unit/test_zombie_death_flow.gd` 的 fixture 代码。coding agent 的 "绝不写入测试目录" 铁律在 REFACTOR 模式中被违反。机制层：REFACTOR prompt 中包含了边界违规清单（要求修复 anti-pattern），但该 anti-pattern 的修复涉及测试 fixture 的改动。coding agent 正确地识别了需要改 fixture，但没有意识到这违反了自身隔离规则。缺少 "REFACTOR 跨边界检测" — 应该在修改前确认所有目标文件都在源码目录下。 | 在 coding.md REFACTOR 模式的检查清单中增加 **Step R0: 边界检查**：列出所有待修改文件，确认无文件在 `test/` 目录下。如有，停止并向 exec 报告——测试 fixture 的修复应由 test-agent 执行，不由 coding agent 执行。同时更新 exec/SKILL.md 的 REFACTOR prompt 模板，在 spawn 时明确标注 "不修改 test/ 目录下的文件"。 | agents/coding.md 关键规则 #1 + REFACTOR 模式章节 |
| 31 | coding: 不允许 pass | ❌ | zombie_base.gd:251 的 `pass` 被 AI-2 GREEN agent 写入（abstract stub），后续 REFACTOR 标记为 "合法保留"。coding.md 规则 #2 说 "不允许 `pass`"，没有 abstract stub 例外。机制层：coding.md 的禁止清单过于绝对，没有定义合法例外。对于 `@abstract` 类的 stub 方法，GDScript 要求必须有 body（不能纯声明），`pass` 是唯一合法写法。规则与语言现实冲突。 | 更新 coding.md 规则 #2：将 "不允许 `pass` 语句（空方法/空分支）" 改为 "不允许 `pass` 语句，唯一例外：`@abstract` 方法的 stub body（GDScript 不支持纯虚方法声明）。空分支用 `return` 替代 `pass`。" | agents/coding.md 关键规则 #2; GDScript 语言限制（Abstract classes 不支持纯虚方法） |
| 32 | collect-lessons: 事实验证 | ❌ | collect-lessons 第4条经验 "ext_resource 配 .tres 缺 .uid 文件时删 uid 字段" 标记为 `[待验证]`，因为 "官方文档未覆盖 UID fallback 行为"。机制层：collect-lessons 的验证流程依赖 "按 config.md 文档节指定的方式查阅文档确认"，但 Godot UID fallback 行为是 Godot 引擎的实现细节，不在官方文档覆盖范围。属于框架行为探索型经验，不适合套用 "文档确认" 流程。 | 在 collect-lessons.md 步骤6的分类规则中增加：框架行为探索型经验（如引擎 UID 解析、引擎内部条件判断行为）标注 `[引擎行为]` 而非 `[待验证]`，不需要文档确认。`[待验证]` 仅用于可文档化的 API 语法/用法问题。 | skills/collect-lessons/SKILL.md 步骤6 分类规则 |
| 33 | RED 阶段修订 agent（额外步骤） | ❌ | AI-4 RED 测试契约设计错误导致需要额外的 agent spawn。根因同 #24 — test-agent 没有对照 plan.md 验证测试契约。额外步骤本身是症状，根因是缺少契约对齐门。 | 同 #24 的方案 — 在 test-agent RED mode 增加设计契约对齐步骤后，此类额外 agent spawn 应不再发生。 | agents/test-agent.md RED Mode |
| 34 | RED-fix agent（额外步骤） | ❌ | AI-2 RED test-agent 用了 `GDScript.can_instantiate()` 检测 `@abstract` 类（期望返回 false），但 Godot 4.7 中此方法对 @abstract 类仍返回 true。这不是语法错误（test-agent 的 self-correction domain），而是 test-agent 对框架 API 行为的理解偏差。机制层：test-agent 缺少 "框架 API 行为验证" 步骤 — 对不确定的 API 应先写最小验证测试确认行为，再基于确认结果写正式测试。 | 在 test-agent.md RED mode 的 Step 1 (Gather identifiers) 后增加 **Step 1b: API 行为确认**：对测试中将使用的框架 API（如 `can_instantiate()`、`is_abstract()` 等），如果 test-agent 不确定其行为，先写一个最小验证脚本确认 API 实际行为，再基于确认结果设计测试。这能避免 "RED 测试本身用了错误 API" 的问题。 | agents/test-agent.md RED Mode；references/godot/config.md 已知坑 |
| 35 | AI-6 VERIFY+code-review 合并 | ❌ | AI-6 的 VERIFY agent (agent-a91251b2ee87ffc94) 被要求同时做 "独立代码审计"，超出了 test-agent 的职责范围。exec/SKILL.md 定义 VERIFY 为 "spawn test-agent → 独立验证门（跑测试，不分析）"。但 collect-lessons-summary.md 第5条指出："主会话要审 agent 的 GREEN 改动，不只是 verify '数字对了'...主会话必须在 verify 加'代码审计'环节"。机制层：exec 的边界检查（6e）只检查资源引用/节点路径/空代码/文件路径等技术指标，不检查代码质量 anti-pattern（如 "为某条测试路径特化"的 dead branch）。代码质量审查被挤到了 VERIFY agent 身上，而不是 exec 主会话的边界检查环节。 | 将 "代码质量 anti-pattern 检测" 纳入 exec/SKILL.md 步骤6e 边界检查的通用检查项。新增检查项：dead branch（为特定测试路径存在的条件分支）、lazy-register anti-pattern（production 代码为测试 fixture 兼容而添加的兜底逻辑）、规范自查（对照 coding.md 规则 #2-#8）。这样 VERIFY agent 回归纯测试执行，代码审计由 exec 主会话在边界检查阶段完成。 | skills/exec/SKILL.md 步骤6e 边界检查；collect-lessons-summary.md 第5条经验 |
| 36 | write-tutorial: .claudeignore 排除 | ❌ | write-tutorial 步骤4b 要求检查并确保 TUTORIAL.md 和 TODO-BEST-PRACTICES.md 在 `.claudeignore` 中。机制层：write-tutorial 的步骤4b 是 "确保 .claudeignore 排除教学文件"，包含具体的 grep 检查和追加逻辑。但该步骤未被有效执行——`.claudeignore` 存在但缺少这两个条目。可能是 write-tutorial 步骤4b 的 bash 命令被跳过或执行后未生效。 | 在项目的 `.claudeignore` 中追加 `TUTORIAL.md` 和 `TODO-BEST-PRACTICES.md` 两行。同时建议在 write-tutorial/SKILL.md 步骤4b 增加验证步骤：追加后再次 grep 确认条目已存在。 | skills/write-tutorial/SKILL.md 步骤4b |
