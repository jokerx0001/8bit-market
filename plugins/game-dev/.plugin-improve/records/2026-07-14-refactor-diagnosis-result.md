# game-dev - refactor 链路诊断结果
## 日期：2026-07-14

### 链路拓扑

```
refactor-conductor/SKILL.md (入口)
├── artifact-manager/SKILL.md                             [skills/artifact-manager/SKILL.md]
├── design-ui/SKILL.md                                     [skills/design-ui/SKILL.md] (条件触发，本次未触发)
├── impact-format.md                                       [skills/refactor-conductor/references/impact-format.md]
├── plan/SKILL.md                                          [skills/plan/SKILL.md]
│   ├── domain-modeling/SKILL.md                           [skills/domain-modeling/SKILL.md]
│   │   └── domain-design-format.md                        [skills/domain-modeling/references/domain-design-format.md]
│   ├── architecture/SKILL.md (--task 模式)                [skills/architecture/SKILL.md]
│   │   └── architecture-format.md                         [skills/architecture/references/architecture-format.md]
│   ├── plan-format.md                                     [references/plan-format.md] (CLAUDE_PLUGIN_ROOT)
│   ├── visual-spec-format.md                              [references/visual-spec-format.md] (CLAUDE_PLUGIN_ROOT)
│   └── superpowers:brainstorming                          [外部 skill]
└── exec/SKILL.md                                          [skills/exec/SKILL.md]
    ├── exec-prompts.md                                    [skills/exec/references/exec-prompts.md]
    ├── exec-logging.md                                    [skills/exec/references/exec-logging.md]
    ├── coding.md (agent)                                  [agents/coding.md]
    │   └── godot/coding.md, testing.md, config.md ...     [references/godot/]
    ├── test-agent.md (agent)                              [agents/test-agent.md]
    │   └── godot/config.md, testing.md, screenshot.md     [references/godot/]
    ├── collect-lessons/SKILL.md                           [skills/collect-lessons/SKILL.md]
    └── write-tutorial/SKILL.md                            [skills/write-tutorial/SKILL.md]
```

所有路径均经 `ls` 验证存在。

---

### 逐步骤诊断

| # | 所属节点 | 应有步骤 | 要求(来自plugin文件) | 实际步骤(来自log/产物) | 达标? | 达标证据 / 不达标点 |
|---|---------|---------|---------------------|------------------|-------|-------------------|
| 1 | refactor-conductor/SKILL.md | 步骤0b: 读config获取dev_dir | "读 `${CLAUDE_PLUGIN_ROOT}/references/{tech}/config.md` 的 `## 产物目录` 节，提取 `dev_dir` 值，回显确认后才能调用 artifact-manager。不猜测不缩写。" (refactor-conductor/SKILL.md:36-38) | 产物目录为 `.godot-dev/refactor-2`，与 config.md `dev_dir: .godot-dev/` 一致 | ✅ | 产物路径 `.godot-dev/refactor-2` 与 config.md（references/godot/config.md:27-29）中 `dev_dir: .godot-dev/`、任务目录格式 `.godot-dev/{kind}-{N}/` 完全一致 |
| 2 | refactor-conductor/SKILL.md | 步骤0c: 创建任务目录 | "Skill({skill: 'game-dev:artifact-manager', args: '--task-dir {dev_dir}/refactor-{N} --kind refactor --dev-dir {dev_dir}'})" (refactor-conductor/SKILL.md:43) | 任务目录 `.godot-dev/refactor-2/` 已创建，progress.json 中 `kind: refactor`、`task: refactor-2` | ✅ | progress.json 中 `"task": "refactor-2"`, `"kind": "refactor"`, `"phase": "exec"`，目录结构完整 |
| 3 | refactor-conductor/SKILL.md | 步骤1c: 分析影响范围 | "充分阅读现有代码：读取受影响的文件、使用 Glob/Grep 发现关联文件、识别当前实现模式、查找已有测试文件、检查测试基础设施状态、评估级联影响和风险" (refactor-conductor/SKILL.md:72-79) | impact.md 存在，包含当前实现摘要（含关键文件列表、数据 schema 描述）、修改范围、排除范围、已有测试（8个测试文件）、风险点（10条）、测试基础设施状态 | ✅ | impact.md 第14-27行有当前实现摘要，第30-41行有修改范围硬约束表，第43-53行有排除范围表，第56-66行有已有测试列表（8个文件），第68-83行有10条风险点，第85-89行测试基础设施状态"已就绪" |
| 4 | refactor-conductor/SKILL.md | 步骤2: 写入impact.md | "按 `references/impact-format.md` 格式写入 `{task_dir}/impact.md`" (refactor-conductor/SKILL.md:83) | impact.md 存在，结构完整 | ✅ | impact.md 包含 impact-format.md（skills/refactor-conductor/references/impact-format.md）规定的所有章节：重构目标、当前实现摘要、修改范围、排除范围、已有测试、风险点、测试基础设施、特殊约束 |
| 5 | refactor-conductor/SKILL.md | 步骤3: 调用plan | "调用 `game-dev:plan` skill。plan 会读取 impact.md 获取约束，在约束范围内用 brainstorming 分析+设计，自己编写自包含的 plan.md" (refactor-conductor/SKILL.md:93-96) | plan.md 存在，结构完整，包含概述、领域模型、行为列表、设计摘要、影响范围、任务列表、测试策略 | ✅ | plan.md 结构完整，包含 plan-format.md 规定的全部章节。plan.md 第26行有"领域模型"节，第38-43行有"设计摘要"，第46-56行有"影响范围"，第60-66行有任务列表 |
| 6 | refactor-conductor/SKILL.md | 步骤5: 调用exec | "Skill({skill: 'game-dev:exec', args: '--mode refactor --task-dir {dev_dir}/refactor-{N}'})" (refactor-conductor/SKILL.md:108) | progress.json phase 为 "exec"，3个AI任务全部 done | ✅ | progress.json 中 `"phase": "exec"`，3个 AI 任务全部 `"status": "done"` |
| 7 | refactor-conductor/SKILL.md | 步骤6: 架构文档更新 | "Skill({skill: 'game-dev:architecture', args: '--update --from {task_dir} --tech {tech}'})" (refactor-conductor/SKILL.md:120) | 未在 log 中直接可见，但 exec completion gate 需 collect-lessons + write-tutorial | ⚠️ | 风险：refactor-conductor 声明了步骤6但 exec 的 completion gate（exec/SKILL.md:349-356）未包含 architecture --update。如果 exec 完成后 conductor 未显式调用 architecture --update，项目级 architecture.md 将不会合并本次变更 |
| 8 | plan/SKILL.md | 步骤3: 加载必须文件 — grill-interview.md | "`{task_dir}/.work/grill-interview.md` — 用户原始描述上下文" (plan/SKILL.md:37-38) | grill-interview.md 存在，包含结构化的用户描述、关键概念提取、与现有架构关系、关键决策点、排除范围 | ❌ | **问题1**：refactor-conductor 工作流中无"grill"步骤（无 brainstorming、无 grill-interview 创建指令）。grill-interview.md 存在但 refactor-conductor 未声明由谁创建。**问题2**：对比 grill-interview.md 内容与 impact.md，grill-interview.md 的关键概念提取表（第16-27行，含"工程含义"列如 `NodePath`、`@export`、`main.tscn`）与 impact.md 的"当前实现摘要"（第14-26行）大量重合，表明 grill-interview.md 是在 impact.md 之后或同时创建的，而非独立的前置步骤 |
| 9 | plan/SKILL.md | 步骤3: 加载 requirements.md | "读取`{task_dir}/.work/requirements.md` 该文件是本次feat的需求文档, 是本次任务的主要功能。包含本次 feat 的新增行为清单和边界规则。其中的行为清单直接作为 plan.md 的'行为列表'输入。" (plan/SKILL.md:42) | requirements.md 存在，包含背景、14条功能行为、9条边界规则、与现有架构关系 | ❌ | **行为清单含大量引擎技术细节**。行为 #1: "Director 启动后,经过 initial_delay 秒"（字段名 `initial_delay` 是实现细节）；行为 #7: "push_warning,跳过该 spawn_point"（`push_warning` 是 Godot API）；行为 #9: "auto_start 必须被强制设为 false"（`auto_start` 是字段名）；行为 #13: "都通过 .tres 编辑"（`.tres` 是 Godot 格式）；行为 #14: "NodePath 方式绑定"（`NodePath` 是 Godot 类型）。违反了 plan-format.md 的"用行为语言"规则："描述'用户/designer 做什么 → 看到什么/发生什么'" |
| 10 | plan/SKILL.md | 步骤4: 领域设计 — 委托 domain-modeling | "该 skill 读取 requirements.md，逐行为识别领域模式（状态机 / 资源管理 / 事件队列 / ...），分析核心规则和边界情况，输出 `{task_dir}/.work/domain-design.md`" (plan/SKILL.md:67-71) | domain-design.md 存在，划分为3个模块 | ❌ | **"容错与配置校验"不是独立模块**。domain-design.md 第16-18行的模块划分表中将"容错与配置校验(Fault Tolerance)"列为第3个模块，但第17行划分理由自述："容错与配置校验横切: 它不是一个独立功能,而是 Director Script + SpawnPoint Controlled Mode 的健壮性约束"。这违反 domain-modeling/SKILL.md 第62行的模块划分标准："判断标准：这个模块能不能作为一个完整功能单元独立交付、独立验证？"——容错规则无法脱离 Director 和 SpawnPoint 独立交付和验证 |
| 11 | plan/SKILL.md | 步骤6: 任务拆分 — 不含代码符号 | "不含代码符号：class 名、方法名、函数名、引擎类型名不出现在描述中——那些是实现方案，不是行为" (plan-format.md:152) | plan.md 任务列表在代码块中，AI-1 描述含 "schema"、"Inspector"、"wave 敌人数据"；AI-2 含"节点"、"idle"、"wave" | ❌ | AI-1 描述含 "Inspector 中可编辑"（引擎工具名）、"schema"（技术术语）、"wave 敌人数据"（代码概念 `Wave`）；AI-2 含"节点"（引擎概念 `Node`）、"idle"（状态机状态名）；AI-3 含"wave"（代码概念）。虽然这些比上一轮有所改善，但仍含引擎/代码领域术语 |
| 12 | plan/SKILL.md | 步骤6: 任务拆分 — 不含文件路径 | "文件名不出现在描述中" (plan-format.md:151) | plan.md 任务描述中未出现 `.gd`/`.tscn`/`.tres` 文件路径 | ✅ | grep `\[AI-\d+\].*\.(rpy\|gd\|tscn\|tres)` 零命中 |
| 13 | plan/SKILL.md | 步骤6: 任务拆分 — 每个AI任务单一类型 | "硬约束：每个 task 只能是单一类型。logic / visual / ui 不允许合并到同一个 task 中。" (plan-format.md:93) | 3个AI任务全部为 `(type: logic)`，无合并 | ✅ | plan.md 第62-66行：AI-1 `(type: logic)`、AI-2 `(type: logic)`、AI-3 `(type: logic)`，类型单一 |
| 14 | plan/SKILL.md | 步骤6: 任务拆分 — 不含测试任务 | "没有'编写/更新测试'任务——测试在各模块的 TDD 循环中自然产出" (plan-format.md:153) | 3个AI任务全部为 logic 类型，无测试任务 | ✅ | 无"编写测试"/"回归测试"任务出现 |
| 15 | plan/SKILL.md | 步骤6: 任务列表格式 | plan-format.md 模板要求 `- [AI-N] (type: logic\|visual\|ui) {描述} (依赖: AI-X, AI-Y)` 为普通 markdown 列表项（plan-format.md:78-81） | plan.md 第60-66行将任务列表包裹在 markdown 代码块（```）中 | ❌ | plan.md 第60行用 ``` 开启代码块，第66行用 ``` 关闭。plan-format.md 模板（第78-81行）中 `- [AI-N]` 是普通 markdown 列表项，不是代码块。exec 的解析规则（plan-format.md:190-198）依赖正则 `- \[AI-(\d+)\]` 匹配——代码块内的文本语法上仍是 markdown 内容，所以 exec 可能仍能解析，但这是格式偏离 |
| 16 | plan/SKILL.md | 步骤9: 格式自检 — 禁止内容清单 | "输出前对照 plan-format.md 的'格式校验清单'逐项确认，然后执行'禁止内容清单'中的所有 grep 命令。全部零命中方可输出。" (plan/SKILL.md:271) | plan.md 通过了基本结构检查 | ⚠️ | 风险：plan-format.md 规定了大量禁止内容 grep（如 PascalCase 标识符检测、Godot API 检测、snake_case 检测），但 plan.md 任务描述中仍含引擎相关术语。无法从 log 确认这些 grep 是否实际执行且零命中 |
| 17 | plan/SKILL.md | 步骤6: 任务描述 — 用行为语言 | "用行为语言：描述'用户/designer 做什么 → 看到什么/发生什么'，不用技术名词（'创建 class'、'定义动画'）开头" (plan-format.md:150) | AI-1 以"实现"开头、AI-2 以"实现"开头、AI-3 以"扩展"开头 | ⚠️ | 任务描述以技术动词（"实现"、"扩展"）而非行为动词（"玩家/designer做什么 → 看到什么"）开头。虽然内容部分描述了功能行为，但开头动词偏技术实现视角 |
| 18 | domain-modeling/SKILL.md | 步骤2: 划分功能模块 | "划分依据是可独立建设、独立验证，不是按领域模式拆分" (domain-modeling/SKILL.md:62) | 划分为3个模块，第3个"容错与配置校验"自述"不是一个独立功能" | ❌ | 同 #10。domain-design.md 划分理由（第17行）明确承认容错"不是一个独立功能,而是...健壮性约束"，却仍被列为独立模块。domain-modeling 步骤2（第81行）要求"划分依据是可独立建设、独立验证"——横切关注点不满足此标准 |
| 19 | domain-modeling/SKILL.md | 步骤2: 防遗漏自查 | "用需求文档的'新增行为'清单逐条核对——每条行为能否归属到某个模块？有没有行为找不到归属？" (domain-modeling/SKILL.md:87) | 模块划分表（第7-11行）将行为 #7、#8 归属到"容错与配置校验"模块 | ❌ | 行为 #7（越界容错）和 #8（空waves容错）被归属到"容错与配置校验"模块而非它们所属的功能模块（#7→Director Script，#8→Director Script）。这导致这些行为的领域分析脱离了它们的功能上下文 |
| 20 | domain-modeling/SKILL.md | 步骤3: 领域描述不出现具体技术 | "领域描述不出现具体技术" (domain-modeling/SKILL.md:102) | domain-design.md 整体使用引擎无关语言 | ✅ | domain-design.md 未出现 Screen、Label、Node、Signal、@export、GDScript 等引擎概念。使用了"状态机"、"命令模式"、"事件聚合"等引擎无关术语 |
| 21 | domain-design-format.md | 禁止内容 | "引擎术语（Screen、Label、Node、Signal、@export、GDScript 等）、代码符号（class 名、方法名、变量名）、实现细节" (domain-design-format.md:66-68) | domain-design.md 中"业界通用做法细节"小节含 `push_warning`（Godot API） | ⚠️ | domain-design.md 第123行出现 `push_warning`（Godot API 调用），第124行 `push_error`。这些出现在"容错与配置校验"模块的"业界通用做法细节"中。虽然是描述"业界做法"，但使用了具体引擎 API 名称，与 domain-design-format.md 的"引擎术语"禁止规则有轻微冲突 |
| 22 | exec/SKILL.md | 步骤2: 只读plan.md | "只读 `{task_dir}/plan.md`。不读 `.work/` 下的任何文件——plan.md 是 exec 读取的唯一设计文档。" (exec/SKILL.md:92) | exec 的 TDD 循环正常执行，agent 各自读取设计文档 | ✅ | progress.json 显示 3 个 AI 任务全部 done。exec 本身不读 .work/，agent spawn 时各自读取所需文档，符合隔离设计 |
| 23 | exec/SKILL.md | 步骤4: 确认测试环境 | "硬门：测试运行器必须可用。测试目录必须存在。已知坑必须处理。" (exec/SKILL.md:104) | 测试全部通过（全量测试），AI-1/AI-2/AI-3 均有 VERIFY 阶段确认 | ✅ | tdd-iterations.md Iter 3 (AI-1 VERIFY): 全量通过；Iter 9 (AI-2 VERIFY): 全量通过；Iter 4 (AI-3 VERIFY): 全量通过 |
| 24 | exec/SKILL.md | 步骤6b: RED — spawn test-agent | "测试文件已创建（路径：___）、RED report 中所有 testcase 都失败了且原因正确（非语法错误、非标识符错误）、没有 mock、假代码" (exec/SKILL.md:182-185) | AI-1 RED: 16 testcases, 0 self-correction rounds；AI-2 RED: 20 testcases, 2 self-correction rounds；AI-3 RED: 17 testcases, 1 self-correction round | ✅ | tdd-iterations.md：AI-1 Iter 1 "Self-correction rounds: 0/3 (RED 一次达成,失败原因正确)"；AI-2 Iter 7 "Self-correction rounds: 2/3" + "Verdict: ✅ 失败原因正确"；AI-3 Iter 1 "Self-correction rounds: 1/3" + "Verdict: ✅ 14 RED 失败原因正确" |
| 25 | exec/SKILL.md | 步骤6b: 创建coding日志目录 | "mkdir -p {task_dir}/.work/coding" (exec/SKILL.md:171) | `.work/coding/` 目录存在但为空（0个文件） | ❌ | `.work/coding/` 目录已创建但**空无一物**。coding agent 的自我验证协议（coding.md:168-170, 273）要求日志写入 `{task_dir}/.work/coding/<testsuite>_run<N>.log`。tdd-iterations.md 中有大量 coding agent 自验证结果（如 AI-2 的 R1/R2/R3 轮次），但这些运行的原始日志（GUT 输出、grep 结果）均未被保存到 `.work/coding/` |
| 26 | exec/SKILL.md | 步骤6c: GREEN — 检查coding-agent未修改test文件 | "未修改 test/ 下文件（检查 coding agent 的已修改文件列表）" (exec/SKILL.md:209) | 边界检查确认 test/ 隔离 | ✅ | AI-1 Iter 4 BOUNDARY-CHECK: "git status 显示 coding agent 只创建 2 个新源文件"；AI-2 Iter 10: "coding agent 只动了 source file"；AI-3 Iter 5: "未触碰 test/ 下任何文件"。但 AI-2 的 test bug 修复在实际执行中由 exec 主会话修改了 test 文件（tdd-iterations.md 第248-252行："Test bug 修复(exec 主会话)"），这破坏了 exec 不应修改 test 文件的隔离原则 |
| 27 | exec/SKILL.md | 步骤6c: GREEN — 检查无pass/TODO残留 | "无 pass / TODO / NotImplemented 残留（grep 已修改的源文件）" (exec/SKILL.md:210) | 边界检查确认零命中 | ✅ | AI-1 Iter 4: "grep pass\|# TODO\|NotImplemented 零命中"；AI-2 Iter 10: "1 命中,位置在 line 175 match _: 默认分支(legitimate no-op)"——确认为合法的 match 默认分支，非占位标记；AI-3 Iter 5: "零命中" |
| 28 | exec/SKILL.md | 步骤6d: VERIFY — 独立验证门 | "全量测试全部通过（test_cmd_full 退出码 0）" (exec/SKILL.md:225) | 3个AI任务均通过 VERIFY | ✅ | AI-1 Iter 3: "全量通过(AI-1 引入 0 新失败)"；AI-2 Iter 9: "全量通过(AI-2 引入 0 新失败)"；AI-3 Iter 4: "AI-3 引入 0 新回归" |
| 29 | exec/SKILL.md | 步骤6e: 边界检查 — exec主会话执行 | "对每个任务强制执行。不是 REFACTOR 的子步骤——即使跳过 REFACTOR，边界检查也必须执行。" (exec/SKILL.md:234) | 3个AI任务全部执行了边界检查 | ✅ | AI-1 Iter 4, AI-2 Iter 10, AI-3 Iter 5 均有完整的 BOUNDARY-CHECK 条目，覆盖：测试文件隔离、空代码/假代码、资源引用完整性、节点路径有效性、信号连接有效性、文件路径有效性 |
| 30 | exec/SKILL.md | 步骤6f: REFACTOR判定 | "边界检查有违规 → 必须 REFACTOR；GREEN 阶段修改了 >1 个文件 → 必须 REFACTOR；GREEN 阶段有 >1 轮自我验证 → 必须 REFACTOR" (exec/SKILL.md:284-289) | AI-1 跳过 REFACTOR，AI-2 执行 REFACTOR，AI-3 跳过 REFACTOR | ✅ | AI-1: 零违规 + 1轮自验证 + 2个新文件（<50行纯data schema）→ 跳过合理。AI-2: 零违规但 3轮自验证 → 必须 REFACTOR → 已执行。AI-3: 零违规 + 1轮自验证 + 1个修改文件 → 跳过合理 |
| 31 | exec/SKILL.md | 步骤9: collect-lessons | "调用 `game-dev:collect-lessons` skill" (exec/SKILL.md:320) | 未在 log 中直接确认 | ⚠️ | 风险：exec completion gate 第4条要求 collect-lessons 完成。但 log 中未找到 collect-lessons 的确认输出。progress.json 中无 collect-lessons 状态追踪 |
| 32 | exec/SKILL.md | 步骤10: write-tutorial | "调用 `game-dev:write-tutorial` skill" (exec/SKILL.md:331) | 未在 log 中直接确认 | ⚠️ | 风险：exec completion gate 第5条要求 write-tutorial 完成（feat/refactor）。但 log 中未找到 write-tutorial 的确认输出 |
| 33 | coding.md (agent) | 启动初始化 — 打印初始化摘要 | "打印初始化摘要（用 markdown 代码块，方便排查）" (coding.md:91-111) | tdd-iterations.md 中无 coding agent 的初始化摘要输出 | ⚠️ | 风险：coding agent 的初始化摘要应包含 mode、task_type、tech、task_dir、project、resolved 命令等。这些信息未出现在 `.work/coding/` 中（目录为空），也未出现在 tdd-iterations.md 中。无法验证 agent 是否正确执行了启动初始化 |
| 34 | coding.md (agent) | GREEN — Phase 1/2/3 三层验证 | "GREEN 模式使用三层结构：初步运行 → 逐个 testcase 循环 → 收尾" (coding.md:139-154) | tdd-iterations.md 中 AI-2 GREEN 展示了 Phase 2 逐个 testcase 诊断（R1→R2→R3） | ✅ | AI-2 tdd-iterations.md 第200-204行展示了逐轮诊断："R1 18/36 失败 (基线 stub 状态) → R2 7/36 失败 (NodePath 修复) → R3 1/36 失败 (test bug)"。体现了先诊断再修复的原则 |
| 35 | coding.md (agent) | GREEN — 自我验证日志写入 .work/coding | Phase 1 Step 1b: `{log_path}` 为 `{task_dir}/.work/coding/<testsuite>_run<N>.log` (coding.md:168-170) | `.work/coding/` 为空 | ❌ | 同 #25。coding.md 第168-170行明确规定日志路径为 `{task_dir}/.work/coding/<testsuite>_run<N>.log`，但该目录完全为空。这意味着 coding agent 的每次测试运行（Phase 1 全量、Phase 2 单case、REFACTOR 全量）的原始日志均未被保存 |
| 36 | coding.md (agent) | GREEN — 禁止批量修改 | "一次只修一个 testcase 的问题，不顺手重构无关代码，不批量修改" (coding.md:262-265) | AI-2 实现中状态机一次性实现（INITIAL_DELAY → ... → ALL_FINISHED 整条链），非逐 testcase 增量 | ⚠️ | tdd-iterations.md AI-2 GREEN 的"Implementation plan"（第138-146行）描述了完整状态机的一次性实现策略，而非逐 testcase 增量。但考虑到状态机各状态之间存在紧耦合（必须完整链条才能工作），批量子修复在架构上可能是合理的。规则与实际情况存在张力 |
| 37 | coding.md (agent) | Step 2b: 系统性诊断 | "必须包含：错误信息 → 当前代码行为 → 设计要求 → 问题分类 → 根因。缺少任一段落即诊断不合格。" (coding.md:196-231) | AI-2 GREEN 有详细的根因分析 | ✅ | AI-2 tdd-iterations.md 第200-204行展示了逐轮诊断，包含具体错误和根因。AI-2 第208-220行有 test bug 的完整诊断（错误信息 + 当前代码行为 + 设计要求 + 根因） |
| 38 | coding.md (agent) | 关键规则 #2: 禁止空壳/假代码 | "不写空壳/假代码。不允许 # TODO、NotImplementedError、非 abstract 方法中的 pass" (coding.md:696) | 边界检查 grep pass 在 AI-2 有 1 命中（match 默认分支） | ✅ | 边界检查确认该 pass 是 match `_:` 的合法默认分支（GDScript 要求 match 覆盖所有分支），非占位标记 |
| 39 | exec-logging.md | 日志职责分离 | "exec 的日志是判定记录，agent 的日志是工作记录。两者写同一个文件但职责不重叠。" (exec-logging.md:14) | tdd-iterations.md 混合了大量 agent 实现细节和 exec 判定记录 | ❌ | tdd-iterations.md 中 AI-2 GREEN 条目包含大量实现细节文档（如"Implementation plan"第138-146行、"实现细节"第154-197行共 44 行），这些是设计文档内容而非迭代日志。exec-logging.md 规定 GREEN 条目格式为：Files modified + Self-verification rounds + Verdict（+ Key output/Analysis on failure），总共应 <20 行。实际 AI-2 GREEN 条目超过 120 行 |
| 40 | exec-prompts.md | GREEN prompt — 目标testcase列表 | "## 目标 testcase — 每个都代表一个待实现的行为 {从 test-agent RED report 的 '### Testcases' 表格提取的 testcase 名称}" (exec-prompts.md:94-97) | tdd-iterations.md 中 RED report 包含 testcase 列表，GREEN prompt 传递了 testcase 名称 | ✅ | AI-1 RED report 有 16 个 testcase 列表；AI-2 RED report 有 20 个 testcase 列表；AI-3 RED report 有 17 个 testcase 列表。coding agent 的 GREEN 工作均基于这些 testcase |
| 41 | exec/SKILL.md | 步骤6c: GREEN检查 — coding-agent自验证 | "coding-agent 自验证报告显示目标 testsuite 全部通过" (exec/SKILL.md:203) | AI-1: 16/16 PASS；AI-2: 35/36→36/36 (test bug修复后)；AI-3: 17/17 PASS | ✅ | tdd-iterations.md 中各 AI 任务均有自验证结果表，目标 testsuite 全部通过 |
| 42 | refactor-conductor/SKILL.md | 步骤1: UI检测判定 | "涉及创建新模块且明显包含新界面 → 触发 design-ui" (refactor-conductor/SKILL.md:54-55) | 本次为纯逻辑重构，无 UI 变更，design-ui 未触发 | ✅ | plan.md "资源需求"节明确："无新增美术资源(无 visual 任务)。本任务纯逻辑/数据层。"——正确判定不触发 design-ui |
| 43 | plan/SKILL.md | 步骤8: 编写plan.md — visual任务spec检查 | "grep -n '(type: visual' {task_dir}/plan.md | grep -v 'spec:'" (plan/SKILL.md:264) | 无 visual 任务，无需检查 | ✅ | 本次全部为 logic 任务，无 visual/ui 任务 |

---

### 根因分析与解决方案

| # | 应有步骤 | 达标? | 根因 | 解决方案 | 解决方案来源 |
|---|---------|-------|------|---------|-------------|
| 8 | plan步骤3: 加载grill-interview.md | ❌ | **流程缺口**：refactor-conductor 工作流中缺少 grill-interview.md 的创建步骤。feat 流程中 orchestrator 通过 brainstorming 创建此文件，但 refactor-conductor 直接从"分析影响范围"跳到"写入 impact.md"，没有对应的用户意图深度采访环节。plan skill 又假设此文件已存在，导致 plan 阶段自行创建了 grill-interview.md（内容与 impact.md 高度重合）。 | **方案A（推荐）**：在 refactor-conductor 步骤1c之后增加步骤1d："写入 grill-interview.md — 基于步骤1c的代码分析发现，提炼用户原始描述（来自重构命令）+ 关键概念提取 + 与现有架构关系 + 不在范围声明"。grill-interview.md 成为 impact.md 的"用户意图侧"输入，impact.md 是"代码实现侧"约束。**方案B**：修改 plan/SKILL.md 步骤3，增加 refactor 分支："refactor 模式 — grill-interview.md 不存在时，从 impact.md 的'重构目标'和'当前实现摘要'节提取上下文作为替代"。 | refactor-conductor/SKILL.md 工作流图（第21-25行）显示了 `创建目录 → [UI检测] → design-ui → 分析影响范围 → 写入 impact.md → plan` 但缺少 grill-interview.md 的创建步骤。对比 orchestrator 工作流包含了 brainstorming→grill-interview 环节 |
| 9 | plan步骤3: requirements.md行为清单 | ❌ | **缺乏行为语言过滤机制**：requirements.md 的行为清单是从 grill-interview.md 直接推导的。grill-interview.md（第16-27行关键概念提取表）中已经包含了工程含义列（`NodePath`、`@export`、`main.tscn`、`.tres`），这些技术术语未经翻译直接进入了 requirements.md 的行为描述。plan-format.md 第150-152行的"用行为语言"规则只约束 plan.md 的任务描述，没有明确要求 requirements.md 也遵守此规则。 | 在 plan/SKILL.md 步骤3的 requirements.md 读取说明中增加约束："requirements.md 中的'新增行为清单'必须使用行为语言——每条行为描述'玩家/designer 做什么 → 看到什么/发生什么'，不含引擎术语（`.tres`、`NodePath`、`@export`、`Inspector`）、不含代码符号（类名/方法名/信号名/API名如 `push_warning`、`emit`）"。同时在 domain-modeling/SKILL.md 步骤1增加："如果 requirements.md 的行为清单含引擎术语，先翻译为行为语言再进入步骤2"。 | plan-format.md §行为列表（第42-53行）和 §描述写作规则（第146-154行） |
| 10 | plan步骤4: domain-design.md模块划分 | ❌ | **domain-modeling 缺少"横切关注点"排除规则**：domain-modeling/SKILL.md 步骤2（第62行）定义了模块划分标准为"可独立建设、独立验证"，但没有明确禁止将横切关注点（容错、日志、校验）作为独立模块。agent 在划分时正确识别了容错的横切性质（"不是一个独立功能"），但因缺少明确的禁止规则而仍将其列为独立模块。同时步骤2的"防遗漏自查"（第87行）只检查行为归属，不检查模块独立性。 | 在 domain-modeling/SKILL.md 步骤2增加规则："**横切关注点不作为独立模块。** 容错处理、日志记录、配置校验等横切关注点的行为应归属到它们保护的功能模块，其边界规则融入该模块的'边界情况清单'。判断标准：如果删除模块X后，模块Y的功能仍然完整可验证 → X 是横切关注点，不是独立模块。"同时在"防遗漏自查"中增加检查项："有没有模块在划分理由中自述'不是一个独立功能'或'横切'？→ 如有，必须拆散归属到其他模块。" | domain-modeling/SKILL.md §步骤2 划分功能模块（第62-87行） + domain-design-format.md §核心原则（第7行："分模块编写"） |
| 15 | plan步骤6: 任务列表格式 | ❌ | **plan-format.md 模板的格式约束不够明确**：plan-format.md 第78-81行给出了任务列表的模板格式（`- [AI-N] (type: ...) {描述} (依赖: ...)`），但没有明确禁止将任务列表包裹在 markdown 代码块中。代码块是 markdown 的有效语法，解析器仍能提取内容，但这不是模板指定的格式。 | 在 plan-format.md 的任务列表模板上方增加一行明确约束："**任务列表不使用代码块（\`\`\`）包裹。** AI 任务列表项是普通 markdown 无序列表，不是代码块内容。exec 解析规则依赖正则匹配列表项格式，代码块虽然语法上仍可解析但偏离了模板格式。" | plan-format.md §任务列表模板（第73-83行）+ §exec 解析规则（第190-198行） |
| 18 | domain-modeling步骤2: 模块划分 | ❌ | 同 #10 | 同 #10 | 同 #10 |
| 19 | domain-modeling步骤2: 防遗漏自查 | ❌ | **行为归属依赖模块划分**：因为"容错与配置校验"被错误地设为独立模块，行为 #7 和 #8（越界容错、空waves容错）被归属到该模块。正确的归属应该是：行为 #7（越界容错）、#8（空waves容错）归属于"关卡刷新剧本"模块，因为容错是 Director 状态机推进过程中的边界行为。 | 同 #10——修复模块划分后，行为归属自然解决。额外在 domain-modeling/SKILL.md 步骤2的"防遗漏自查"中增加："每条行为的归属模块是否是其功能载体的模块？（容错行为归属到被保护的功能模块，不是单独的容错模块）" | domain-modeling/SKILL.md §步骤2（第87行） |
| 25 | exec步骤6b: .work/coding日志目录 | ❌ | **coding agent 日志写入无强制检查**：exec 步骤6b（第171行）创建了 `.work/coding/` 目录，但步骤6c（GREEN检查）的检查清单（第201-210行）中没有检查该目录是否包含日志文件。coding.md 的自我验证协议（第168-170行）指定了日志路径，但没有强制执行机制——agent 的日志写入依赖 agent 自觉，而非 exec 验证。 | 在 exec/SKILL.md 步骤6c GREEN 检查清单中增加一项："`.work/coding/` 目录包含本轮测试运行日志（至少一个 `<testsuite>_run<N>.log` 文件）"。同时在 coding.md 的"自我验证协议"开头增加铁律："每轮测试运行后**必须**将原始输出保存到 `{task_dir}/.work/coding/<testsuite>_run<N>.log`。不保存日志 = 本轮验证无效。" | exec/SKILL.md §步骤6c GREEN检查清单（第200-213行）+ coding.md §自我验证协议 Phase 1 Step 1b（第168-170行） |
| 35 | coding agent: 日志写入.work/coding | ❌ | 同 #25 | 同 #25 | 同 #25 |
| 39 | exec-logging: tdd-iterations.md格式 | ❌ | **日志职责边界模糊 + coding agent 将 tdd-iterations.md 当作设计文档**：exec-logging.md §日志职责分离（第9-13行）明确规定 exec 写判定记录、coding agent 写工作记录。但 coding agent 在 GREEN 阶段的 tdd-iterations.md 写入中包含了大量实现方案文档（AI-2 的"Implementation plan" 8行 + "实现细节" 44行），这些内容属于 `.work/design.md` 的设计文档范畴，不是迭代日志。根因是 coding.md 的 Step 2c（第238-252行）要求将根因分析写入 tdd-iterations.md，但格式示例中的"根因分析"节被 agent 扩展为完整的实现方案文档。 | 在 coding.md 的 Step 2c 格式模板中增加约束："**根因分析不超过 5 行。** Failure Reason 和 Solution 各 1-2 句话。禁止写入：实现计划、架构决策、代码片段、多段落设计文档。这些属于 .work/design.md。"同时在 exec-logging.md 增加 coding agent GREEN 条目的**最大长度限制**："GREEN 条目（含 exec 判定 + agent 诊断）总计不超过 40 行。" | exec-logging.md §日志职责分离（第7-13行）+ coding.md §Step 2c 记录根因（第238-252行） |
| 7 | refactor-conductor步骤6: 架构文档更新 | ⚠️ | **refactor-conductor 声明的步骤6与 exec completion gate 不一致**：refactor-conductor（第119-120行）声明步骤6调用 `game-dev:architecture --update`，但 exec 的 Completion Gate（exec/SKILL.md:349-356）只要求 collect-lessons 和 write-tutorial，未提及 architecture --update。这意味着如果 exec 完成后 refactor-conductor 未显式调用，项目级 architecture.md 不会合并本次变更。 | 在 exec/SKILL.md Completion Gate 中增加第6条："`game-dev:architecture --update` 已完成（仅 refactor 模式，feat 模式可选）"。或者将 architecture --update 的调用从 refactor-conductor 移到 exec 步骤9.5（在 collect-lessons 之后、write-tutorial 之前），使 exec 统一管理所有 completion 步骤。 | refactor-conductor/SKILL.md §步骤6（第115-123行）+ exec/SKILL.md §Completion Gate（第349-356行） |
| 17 | plan步骤6: 任务描述用行为语言 | ⚠️ | **"行为语言"的标准在 plan-format.md 中有定义但缺少具体示例**：plan-format.md 第150行说"用行为语言：描述'用户/designer 做什么 → 看到什么/发生什么'"，第121-142行有好/坏对照。但 AI-1~3 的任务描述以"实现"开头而非"玩家/designer做什么"，说明 agent 对"行为语言"和"技术描述"的边界判断不够精确。 | 在 plan-format.md 的好/坏对照中增加一组示例，专门针对 logic 任务（现有示例偏向 visual 任务）："❌ 坏: '实现关卡剧本的可配置数据 schema: Inspector 中可编辑初始延迟、若干条波次指令' → 问题：'实现...schema' 是技术动词，'Inspector'是工具名。✅ 好: '关卡设计师在编辑器中配置关卡剧本：设置初始等待时间、按序添加波次指令（每条指令指定倒计时、清场后等待、通知哪些刷新点、敌人配置），无需修改代码'" | plan-format.md §描述写作规则（第146-154行）+ §好/坏对照（第121-142行） |
| 21 | domain-design: 引擎术语 | ⚠️ | **domain-design-format.md 的"禁止引擎术语"规则的边界不够清晰**：第66行禁止"引擎术语（Screen、Label、Node、Signal、@export、GDScript 等）"。`push_warning` 出现在"业界通用做法细节"中，是一种灰色地带——它描述的是"业界怎么做"而非"本方案怎么做"，但使用了具体引擎 API 名称。 | 在 domain-design-format.md 的"禁止内容"中增加澄清："**即使是在'业界通用做法'描述中，也不使用特定引擎的 API 名称。** 用通用术语替代：'记录警告日志'而非 `push_warning`、'发出错误信号'而非 `push_error`。" | domain-design-format.md §禁止内容（第65-68行） |
| 26 | exec步骤6c: test文件隔离 | ⚠️ | **exec 主会话修改 test 文件破坏了隔离原则**：AI-2 发现 test bug（Resource reference equality），coding agent 因隔离规则不能修改 test 文件。exec 主会话直接修改了 test 文件（tdd-iterations.md 第248-252行），但 exec 本身不应修改任何源文件或测试文件——exec 是编排器。正确的处理流程应该是：标记 test bug → 重新 spawn test-agent 修复 → 验证修复后 RED 仍然正确。 | 在 exec/SKILL.md 步骤6c GREEN 检查清单中增加处理规则："如果发现 test 文件自身有 bug（非 coding agent 实现问题）：① 在 tdd-iterations.md 中记录 bug 详情和修复方案；② 重新 spawn test-agent 传入 bug 描述，由 test-agent 修复测试文件；③ 确认修复后重新进入 RED → GREEN。**exec 主会话不直接修改 test/ 或源文件。**" | exec/SKILL.md §核心原则（第12行："exec 不做判断，只做传递"）+ §信息隔离清单（第107-112行） |
| 31 | exec步骤9: collect-lessons | ⚠️ | **Completion Gate 步骤缺少执行确认**：exec Completion Gate 要求 collect-lessons 和 write-tutorial 完成，但 progress.json 只追踪 AI 任务状态（done/pending），不追踪 completion 步骤（collect-lessons、write-tutorial、architecture-update）的执行状态。log 中也无这些步骤的输出确认。 | 在 exec/SKILL.md 中增加：步骤9和步骤10完成后，更新 progress.json 增加 `"completion_steps": {"collect_lessons": "done", "write_tutorial": "done", "architecture_update": "done"}`。或在 tdd-iterations.md 末尾追加 completion 步骤的执行记录。 | exec/SKILL.md §Completion Gate（第349-356行） |
| 32 | exec步骤10: write-tutorial | ⚠️ | 同 #31 | 同 #31 | 同 #31 |
| 33 | coding agent: 初始化摘要 | ⚠️ | **初始化摘要没有持久化要求**：coding.md 第91-111行要求打印初始化摘要到响应文本，但没有要求写入文件。这意味着初始化摘要只在 agent 的响应流中可见，在 `.work/coding/` 目录为空的情况下完全无法追溯。 | 在 coding.md 的启动初始化步骤5增加："将上述初始化摘要**同时**追加到 `{task_dir}/.work/coding/init.log`。" | coding.md §启动初始化 步骤5（第91-111行） |
| 36 | coding agent: GREEN批量修改 | ⚠️ | **状态机实现的内在耦合性与逐testcase增量规则的张力**：coding.md 第262-265行要求"一次只修一个 testcase"，但状态机的各状态（INITIAL_DELAY → PRE_WAVE_DELAY → WAVE_ACTIVE → POST_WAVE_IDLE → ALL_FINISHED）存在紧耦合——实现 IDLE→DELAYING 的 transition 就必须同时实现 Timer 创建、timeout 信号连接和下一状态入口。这不是规则错误，而是规则需要澄清适用范围。 | 在 coding.md 的 Step 2d（实施修复）中增加澄清："**当多个 testcase 共享同一基础架构时**（如状态机链条、共享数据结构），可以先实现基础架构使所有依赖它的 testcase 通过，再逐个处理独立 testcase。基础架构实现必须在诊断中明确说明哪些 testcase 共享同一根因。" | coding.md §Step 2d 实施修复（第258-268行） |
