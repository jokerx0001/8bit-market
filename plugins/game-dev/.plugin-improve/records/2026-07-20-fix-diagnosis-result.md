# game-dev - fix 诊断结果
## 日期：2026-07-20

### 链路拓扑

```
commands/fix.md
└── skills/fix-conductor/SKILL.md
    ├── skills/artifact-manager/SKILL.md
    ├── agents/test-agent.md
    │   ├── references/godot/config.md
    │   ├── references/godot/testing.md
    │   └── references/godot/screenshot.md
    ├── agents/fix-agent.md
    │   ├── skills/fix-loop/SKILL.md
    │   │   ├── skills/debug-root-cause/SKILL.md
    │   │   │   └── skills/visual-qa/SKILL.md
    │   │   │       └── skills/visual-qa/prompts/question.md
    │   │   └── skills/visual-qa/SKILL.md (screenshot验证)
    │   ├── references/godot/config.md
    │   ├── references/godot/style-guide.md
    │   ├── references/godot/project-organization.md
    │   ├── references/godot/coding.md
    │   ├── references/godot/quirks.md
    │   ├── references/godot/docs.md
    │   ├── references/godot/screenshot.md
    │   ├── references/godot/3d-construction.md
    │   └── references/godot/testing.md
    └── references/godot/config.md
```

### 逐步骤诊断

| # | 所属节点 | 应有步骤 | 要求(来自plugin文件) | 实际步骤(来自log) | 达标? | 达标证据 / 不达标点 |
|---|---------|---------|---------------------|------------------|-------|-------------------|
| 1 | skills/fix-conductor/SKILL.md | 阶段0a: 读CLAUDE.md确定tech | "读 CLAUDE.md 确定 tech"（line 51） | 无法从当前log直接验证（主会话transcript不在本log目录中），但从产物检测到 godot config 被正确加载 | ✅ | 产物目录为 `.godot-dev/fix-18`，reference 文件路径均为 godot，说明 tech=godot 检测正确 |
| 2 | skills/fix-conductor/SKILL.md | 阶段0b: 读config获取dev_dir | "读 `${CLAUDE_PLUGIN_ROOT}/references/{tech}/config.md` 的 `## 产物目录` 节，提取 `dev_dir` 值"（line 55-57） | 产物在 `.godot-dev/fix-18/`，与 `references/godot/config.md` line 28-29 声明的 `dev_dir = .godot-dev/` 和 `{kind}-{N}` 格式一致 | ✅ | dev_dir=`.godot-dev` 正确解析，任务目录 `.godot-dev/fix-18/` 创建正确 |
| 3 | skills/fix-conductor/SKILL.md | 阶段0c: 创建任务目录 | "Skill({skill: "game-dev:artifact-manager", args: "--task-dir {dev_dir}/fix-{N} --kind fix --dev-dir {dev_dir}"})"（line 61-63） | 产物目录存在：`.godot-dev/fix-18/` | ✅ | 目录已创建，counter 正确递增到 18 |
| 4 | skills/fix-conductor/SKILL.md | 阶段1: 行为澄清—解析BUG描述并询问预期行为 | "解析用户的 BUG 描述，检查是否已包含预期行为，向用户询问预期行为"（line 79-86） | requirements.md 包含完整的行为澄清（3个BUG拆分为5条预期行为，逐条标注验证方式） | ✅ | requirements.md:1-32 包含 BUG 描述原文 + 5 条预期行为，每条标注 verification 方式（screenshot/behavior） |
| 5 | skills/fix-conductor/SKILL.md | 阶段1: 硬门—用户确认预期行为 | "用户已确认预期行为（回复 OK / 明确确认）"（line 122） | requirements.md 存在且内容完整 | ✅ | requirements.md 内容非空（1629 bytes），行为清单完整 |
| 6 | skills/fix-conductor/SKILL.md | 阶段1b: 视觉关键词检测grep | "echo "{BUG 描述} {预期行为}" \| grep -iE "显示\|渲染\|画面\|..." 此 grep 命令必须执行。跳过 = 违反铁律。"（line 139-145） | 无法从当前log直接验证grep是否执行（grep 命令为 bash 执行，不在 agent JSONL 中）。但 requirements.md 中正确识别了 screenshot 验证需求 | ⚠️ | 风险：grep 命令是否执行无法从 subagent log 验证。grep 是 bash 命令，不在 agent JSONL transcript 中。产物（requirements.md 含 screenshot 标注）表明视觉检测结果正确，但无法确认是否通过 grep 机械检查还是人工判断。建议在 fix-conductor 中要求将 grep 结果写入 requirements.md 或显式 log 作为证据。 |
| 7 | skills/fix-conductor/SKILL.md | 阶段1c: 写入requirements.md | "cat > {task_dir}/.work/requirements.md << 'EOF' ... cat 验证内容非空"（line 177-192） | requirements.md 存在，内容非空 | ✅ | requirements.md:1-32, 1629 bytes，包含 BUG 描述、5 条预期行为、verification 方式 |
| 8 | skills/fix-conductor/SKILL.md | 阶段2: spawn test-agent 写BUG复现测试 | spawn prompt 必须包含: 模式 RED, project, task_dir, BUG描述, 预期行为（含验证方式）, "不得在 prompt 中包含任何根因分析"（line 199-230） | test-agent (agent-ab2ab938) user message 包含: mode RED, project zombies-demo, task_dir .godot-dev/fix-18, BUG description, expected behaviors | ✅ | agent-ab2ab938.jsonl user message 完整包含所有必填字段，无根因分析内容 |
| 9 | skills/fix-conductor/SKILL.md | 阶段2 硬门: 测试必须FAIL | "测试必须 FAIL。如果 PASS → 检查测试是否真正覆盖了 BUG 场景"（line 233-235） | test-agent RED report 显示 13 GUT + 5 screenshot 测试全部 FAIL | ✅ | agent-ab2ab938.jsonl L238 RED report: GUT 13 testcases 全部正确失败，screenshot 5 scripts 产出 |
| 10 | skills/fix-conductor/SKILL.md | 阶段2 硬门: screenshot行为必须有截图脚本+.question产出 | "标注为 screenshot 的行为必须有对应的截图脚本 + .question 文件产出"（line 235） | test-agent 产出了 5 对 screenshot script + .question 文件 | ✅ | test/visual/test_fix18_weapon_slot_thumbnails_screenshot.gd + .question 等 5 对文件全部创建 |
| 11 | skills/fix-conductor/SKILL.md | 阶段2→3 硬门: 提取testsuite/testcase列表传入fix-agent | spawn prompt 必须包含 "## 目标 testsuite" 和 "## 目标 testcase"（从 RED report 提取）（line 267-271） | fix-agent (agent-a93a4ff) user message: 包含 project, task_dir, BUG描述, 预期行为 — 但**缺少** `## 目标 testsuite` 和 `## 目标 testcase` 字段 | ❌ | fix-conductor SKILL.md:267-271 声明 spawn prompt 须含 "## 目标 testsuite {从 RED report 提取的 suite 名}" 和 "## 目标 testcase {从 RED report 提取的 testcase 名列表}"。但 fix-agent user message（agent-a93a4ff.jsonl）中无这两个字段。这导致 fix-agent 自行通过 `find` 命令搜索测试文件（L18, L42-43, L234-235），而非从 conductor 获取精确的测试目标列表。 |
| 12 | skills/fix-conductor/SKILL.md | 阶段2→3 spawn prompt 禁止事项: 不含根因分析 | "禁止在 spawn prompt 中写入 '调查结论'、'已知根因'、'不要重新调查'、'直接修'"（line 279） | fix-agent spawn prompt 无根因分析内容 | ✅ | fix-agent user message 只含 project/task_dir/BUG描述/预期行为，无根因 |
| 13 | skills/fix-conductor/SKILL.md | 阶段3 spawned fix-agent | "fix-agent 启动后读取参考文件 → 调用 Skill("game-dev:fix-loop")"（line 283-284） | fix-agent 启动后读取了全部 8 个 godot reference 文件 + requirements.md → 调用 Skill("game-dev:fix-loop") | ✅ | agent-a93a4ff.jsonl L4-L30: 读取 config/requirements/style-guide/project-organization/coding/quirks/docs/screenshot/testing → L82: Skill(game-dev:fix-loop) |
| 14 | agents/fix-agent.md | 启动初始化: 打印初始化摘要(Hard Gate) | "Hard Gate: spec_files_read 表格必须输出。每一个 ❌ (不存在) 必须有原因说明。"（line 92） | 初始化摘要已输出（L81），但缺少 screenshots 命令解析字段 | ⚠️ | fix-agent 输出了初始化摘要，列出了所有 spec_files_read 状态。但摘要中缺少 `screenshot 命令` 字段（agent.md:89 声明应包含此字段）。未影响执行——screenshot 命令后续通过 read screenshot.md 正确获取。 |
| 15 | agents/fix-agent.md | 启动初始化: 调用fix-loop | "Skill({skill: "game-dev:fix-loop", args: "--task-dir ... --bug-description ... --expected-behaviors ... --suite ... --testcases ..."})"（line 97） | fix-loop 被调用但 args 中缺少 --suite 和 --testcases 参数 | ❌ | agent.md:96-98 声明 fix-loop 调用须传 `--suite {suite名}` 和 `--testcases {testcase名列表}`。但 agent-a93a4ff.jsonl L82 的 Skill 调用只有 `--task-dir`、`--bug-description`、`--expected-behaviors` 三个参数，缺少 `--suite` 和 `--testcases`。这直接导致 fix-loop 无法精确知道要验证哪些测试，只能依赖 debug-root-cause 中的代码分析。根因：fix-conductor 阶段 2→3 spawn prompt 未传入 testsuite/testcase → fix-agent 收到 prompt 中无此信息 → 调用 fix-loop 时也无此参数。 |
| 16 | skills/fix-loop/SKILL.md | Step 2: 根因分析—调用 debug-root-cause | "调用 Skill("game-dev:debug-root-cause") 进行根因分析"（line 38） | fix-loop 正确调用了 debug-root-cause skill | ✅ | agent-a93a4ff.jsonl L93: Skill(game-dev:debug-root-cause, args: --task-dir ... --bug-description ...) |
| 17 | skills/fix-loop/SKILL.md | Step 3: 获取根因—读 debug-analysis.md | "debug-root-cause 产出 debug-analysis.md。读取分析结果"（line 43-44） | debug-analysis.md 被写入后，fix-loop 继续 Step 4 | ✅ | debug-analysis.md 存在 (5766 bytes)，被 fix-loop 正确引用 |
| 18 | skills/debug-root-cause/SKILL.md | 步骤1: 确认BUG存在—跑测试捕获失败输出 | "跑 BUG 复现测试，捕获确切的失败输出：记录哪个 assertion 失败、actual vs expected、完整 traceback"（line 33-43） | debug-root-cause 未执行 GUT 测试。直接从读源代码开始 | ❌ | SKILL.md:33-43 要求先跑测试捕获失败输出。但 agent-a93a4ff.jsonl L97-L148 显示 debug-root-cause 直接从 `find scripts` (L98) 和阅读源代码 (L103-L148) 开始，从未执行任何 GUT 测试命令。debug-analysis.md 的"证据链"§1 (line 83-84) 写的也是 "代码 reading 确认" 而非测试失败输出。违反了 "还没跑测试就开始写 debug-analysis.md" 这条 Red Flag（line 160）。 |
| 19 | skills/debug-root-cause/SKILL.md | 步骤2: 逆向追踪 | "从失败点出发，逐层往上追，直到找到'正确输入变成错误输出'的转换点"（line 52-63） | debug-root-cause 执行了代码阅读追踪 | ✅ | agent-a93a4ff.jsonl L98-L160：系统性地读了 inventory_display.gd, item_inventory.gd, weapon_inventory.gd, character_base.gd, grenade.gd, grenade_controller.gd, weapon_data.gd, hud.gd, item_data.gd, item_base.gd, item_hotkey.gd, grenade_projectile.gd，以及相关的 .tscn 和 .tres 文件。逆向追踪从 UI 渲染层逐层追到数据层和逻辑层。 |
| 20 | skills/debug-root-cause/SKILL.md | 步骤4: 最小验证 | "用最小的临时修改验证假设，确认后立即撤销。验证修改是 hack——不要把它写成正式修复。git checkout / 手动还原"（line 102-113） | debug-root-cause 未执行最小验证+撤销流程 | ❌ | SKILL.md:104-113 要求：1) 做最小修改 2) 跑测试确认 3) 撤销修改。agent-a93a4ff.jsonl L178-L179 显示：代码分析完成后直接 "Write debug-analysis.md"，无任何临时修改、测试运行、git revert 操作。debug-analysis.md "证据链"§3 (line 88) 标注的是 "代码 reading 确认" 而非 "最小验证结果"。违反了 "步骤 4 的临时修改必须撤销。debug-analysis.md 不含实现代码"（line 187）。 |
| 21 | skills/debug-root-cause/SKILL.md | 步骤5: 写入debug-analysis.md格式 | debug-analysis.md 必须包含: BUG描述、预期行为(含验证方式)、根因、证据链(4项)、影响范围表、修复方向（line 117-148） | debug-analysis.md 已写入，格式基本完整 | ✅ | debug-analysis.md:1-108 包含所有必需章节。但证据链§3 标注为 "最小验证结果" 而实际内容是 "代码 reading 确认"——内容与章节名不匹配（与 #20 的跳过步骤4 一致） |
| 22 | skills/fix-loop/SKILL.md | Step 4: 记录本轮诊断到 fix-attempts.md | "追加到 fix-attempts.md：根因、修复思路"（line 47-57） | fix-attempts.md 已写入 | ✅ | fix-attempts.md:1-10 包含第 1 轮根因和修复思路 |
| 23 | skills/fix-loop/SKILL.md | Step 5: 实施修复—代码修改规则 | "遵循已读取的规范文件；不修改测试文件；不写空壳/假代码；API不确定时查官方文档"（line 69-74） | fix-agent 实施了代码修改：weapon_data.gd, inventory_display.gd, grenade.gd, item_base.gd, item_inventory.gd, character_base.gd | ✅ | agent-a93a4ff.jsonl L191-L232: 逐个文件 Edit，所有修改符合 coding.md 规范（static typing, @export, const preload）。无测试文件被修改。遇到 compile error 后及时修正（item_base.gd 字段声明冲突 → duck-type 方案）。 |
| 23a | skills/fix-loop/SKILL.md | Step 5: 修复质量—武器缩略图应为真实图标而非纯色方块 | "考虑最佳实践：不是最小 hack，是正确、可维护的修复"（fix-loop.md:65）；coding.md 禁止 pass/空壳/placeholders；quirks.md 列出了已知坑但无 asset-gen 调用指引 | fix-agent 的修复方案：`WeaponData.icon` 字段(null) → `_generate_weapon_placeholder_icon()` 生成纯色方块(MELEE=橙红, PRIMARY=蓝, PISTOL=绿)。所有 .tres 文件的 icon 字段为 null | ❌ | 产物 `inventory_display.gd:274-296`：`_generate_weapon_placeholder_icon()` 按武器类型生成纯色矩形+白色边框。所有武器 .tres (axe.tres, pistol.tres, rifle.tres) 的 icon 字段均为 null。插件有 `references/godot/asset-gen.md` 定义了 mmx image generate 资产生成能力，但 fix 工作流没有任何阶段触发资产生成。修复是"让代码显示占位色块"而非"产生正确的武器缩略图"——这属于把症状（无图标）从"不可见"变成"可见但不正确"，不是完整的修复。根因在流程设计：fix 工作流缺少"需要视觉资产时触发 asset generation"的阶段。 |
| 24 | skills/fix-loop/SKILL.md | Step 6: 验证—跑BUG复现测试 | "GUT 测试：使用 fix-agent 启动初始化中解析的测试执行方法，跑目标 testsuite"（line 80） | fix-agent 运行了 fix-18 GUT 测试 | ✅ | agent-a93a4ff.jsonl L280-L315: 多次运行 GUT 测试。最终 12/12 fix-18 GUT 通过。L315: "全部 12 个 GUT 测试通过！" |
| 24a | agents/fix-agent.md | 启动初始化: 测试执行方法—log_path 重定向 | "全量执行：`{test_cmd_suite} > {log_path} 2>&1`；结果提取：`{test_failure_grep}`（将 `{log_path}` 替换为日志文件路径）"（line 57-59） | 所有 12 次 GUT 测试均用 `\| tail`/`\| grep` 管道丢弃输出，从未 `> {log_path}` 保存。声明的 `{log_path}` 被架空 | ❌ | fix-agent.md:57 声明 `> {log_path}`，但声明后没有 Hard Gate 强制 agent 自检"log_path 是否已替换为实际路径、输出是否已保存"。agent 能看到测试结果就行，不需要证明持久化。**Harness 根因：缺少机制 5 (Hard Gate) 强制兑现已声明的执行方法。** 修复：在测试执行方法声明后增加 Hard Gate——每次测试执行后确认 `{log_path}` 已替换为实际文件路径且输出已保存（非管道丢弃）。 |
| 25 | skills/fix-loop/SKILL.md | Step 6: 验证—全量回归测试 | 修复后跑全量测试确认无回归 | fix-agent 跑了全量测试并做了 git stash 对比 | ✅ | agent-a93a4ff.jsonl L316-L355: 全量测试 + git stash baseline 对比。确认 2 个 "失败" 是预期行为变更（近战弹药显示 / 手雷消耗时机），0 个真正回归 |
| 26 | skills/fix-loop/SKILL.md | Step 6: 验证—screenshot 验证 | "执行截图脚本 → base64 解码保存 → 读 .question → 调用 Skill("game-dev:visual-qa")"（line 82） | fix-agent 加了 `--headless` 导致黑屏，检测到 xvfb 不可用就放弃。但 VERIFY test-agent 在相同环境下不用 `--headless` 成功截图 | ❌ | screenshot.md:5 "绝不能加 `--headless`"。fix-agent 启动时读了 screenshot.md 标记 ✅（L29），但执行截图时（L362, L365）加了 `--headless`。**Harness 根因：渐进式披露违规。** screenshot.md 在启动时与 coding.md 等批量预载，到执行时（几十步之后）约束已不在工作记忆。agent 把 "Read ✅" 当成完成而非承诺。**修复：** screenshot.md 从启动批量列表移除，改为执行截图前即时 Read——读完立即对照约束写命令。 |
| 27 | skills/fix-loop/SKILL.md | 完成: 写fix-summary.md | "写入 fix-summary.md: BUG、根因、修复、验证、尝试轮次"（line 110-130） | fix-summary.md 已写入 | ✅ | fix-summary.md:1-48 包含所有必需章节 |
| 28 | skills/fix-conductor/SKILL.md | 阶段4: VERIFY — spawn test-agent 独立验证 | "Spawn test-agent GREEN mode: 模式 GREEN, project, task_dir, 任务描述。禁止写入显式 Bash 命令"（line 293-320） | test-agent VERIFY (agent-aac6bfa) spawn prompt: mode GREEN, project zombies-demo, task_dir .godot-dev/fix-18, 任务描述 | ✅ | agent-aac6bfa.jsonl user message 符合格式要求，无显式 Bash 命令 |
| 29 | skills/fix-conductor/SKILL.md | 阶段4 验证标准: 全量测试通过 | "全量测试全部通过（test_cmd_full 退出码 0）"（line 312） | VERIFY test-agent 运行了全量测试，fix-18 专属测试全部通过 | ✅ | agent-aac6bfa.jsonl L55-L66: 全量 GUT + fix-18 专属测试。fix-18 GUT: 12/12 通过。全量: 529/556 通过（9 个预先存在的失败 + 2 个预期行为变更） |
| 30 | skills/fix-conductor/SKILL.md | 阶段4 验证标准: screenshot 验证 | "有 screenshot 验证方式的行为：截图验证通过 visual-qa"（line 315） | VERIFY test-agent 成功执行了 2/5 截图 + visual-qa | ⚠️ | agent-aac6bfa.jsonl L136-L154: 2 个截图成功产出 PNG + visual-qa 验证通过。3 个 grenade 相关截图失败（player 加载问题——目标项目代码问题，非插件工程问题）。关键发现：screenshots 在无 xvfb 环境下可行，与 fix-agent 的 #26 诊断矛盾。
| 31 | agents/test-agent.md | RED Step 0: 读取设计文档 | "只读两个文件：{task_dir}/.work/requirements.md, {task_dir}/.work/design.md"（line 86-91） | test-agent (BUG repro) 尝试读取 design.md，文件不存在 | ❌ | agent-ab2ab938.jsonl L8: Read design.md → "File does not exist."。test-agent RED mode Step 0 无条件要求读 design.md（line 89-90），但 fix 工作流只创建 requirements.md，不创建 design.md。design.md 是在 feat/refactor 工作流的 plan 阶段由 design-ui skill 产出的。这导致 test-agent 在 fix 工作流中每次都会遇到文件不存在的错误。虽然 test-agent 正确降级处理了（不影响后续），但该步骤声明存在结构性问题：它假设所有任务类型都有 design.md。 |
| 32 | agents/test-agent.md | RED Step 0: 读取设计文档 (VERIFY 阶段) | VERIFY test-agent 同样尝试读取 design.md | VERIFY test-agent 也遇到 design.md 不存在 | ❌ | agent-aac6bfa.jsonl L8: Read design.md → "File does not exist."。与 #31 相同的问题在 VERIFY 阶段复现。GREEN mode 虽未在 SKILL.md 中显式声明读 design.md，但 test-agent 的 Startup 章节（line 36-39）声明的初始化流程不包含读 design.md——design.md 读取在 RED mode Step 0。因此 VERIFY 阶段读 design.md 是 agent 的自主行为，说明 agent 的初始化流程对不同 mode 缺少明确区分。 |
| 33 | skills/debug-root-cause/SKILL.md | 步骤1: Screenshot 测试确认 | "screenshot 测试：执行截图脚本，base64 解码保存，读 .question，调用 visual-qa"（line 44） | debug-root-cause 跳过了 screenshot 测试的执行 | ❌ | 同 #18 的根因——debug-root-cause 完全跳过了步骤 1。因此不仅 GUT 测试未运行，screenshot 测试也未执行。debug-analysis.md 的证据链缺少任何测试输出。 |
| 34 | agents/fix-agent.md | 代码规范—pass 语句禁止 | "绝不写空壳/假代码。不允许 # TODO、NotImplementedError、非 abstract 方法中的 pass"（line 105） | 代码审查——修复中引入的代码是否有 pass？ | ✅ | 修复代码中未引入 pass 语句。所有方法体实现完整。 |
| 35 | skills/fix-loop/SKILL.md | 步骤7: 判定—全部PASS后退出 | "全部 PASS → 退出循环。跳转到'完成'"（line 86） | fix-18 GUT 全部通过后正常退出循环 | ✅ | agent-a93a4ff.jsonl L380-L389: 写入 fix-summary.md 并输出 Fix Complete 报告 |

### 根因分析与解决方案

| # | 应有步骤 | 达标? | 根因 | 解决方案 | 解决方案来源 |
|---|---------|-------|------|---------|-------------|
| 6 | 阶段1b: 视觉关键词检测grep | ⚠️ | **证据链缺失。** fix-conductor SKILL.md line 139-145 要求 grep 命令"必须执行"，但该命令为 bash 命令，执行后 stdout 不会自动记录到任何持久化文件中。后续阶段无法验证 grep 是否真的执行了，只能从产物（requirements.md 含 screenshot 标注）反推结果正确。**机制层：** 缺少"硬门执行证据"护栏——强制执行的命令没有强制产出证据文件。 | 在 fix-conductor SKILL.md 阶段 1b 的 grep 命令后追加一行：`echo "{grep 匹配结果}" >> {task_dir}/.work/requirements.md`（或写入单独的 `.work/visual-check.log`）。在阶段 1c 的"硬门"检查中增加一条：检查 grep 结果文件是否存在且非空。 | plugin-improve references/harness-methodology.md — 护栏机制 #5 "Hard Gate Evidence": 每个硬门必须有可验证的证据文件，不能仅靠"执行了"。 |
| 11 | 阶段2→3: 提取testsuite/testcase列表传入fix-agent | ❌ | **数据传递断裂。** fix-conductor SKILL.md:239 声明"从 test-agent 的 report 提取 testsuite 名和 testcase 名列表"，line 267-271 的 spawn prompt 模板包含 `## 目标 testsuite` 和 `## 目标 testcase` 字段。但实际 spawn prompt 中缺少这两个字段。**机制层：** fix-conductor 的阶段 2→3 之间缺少硬门检查——"spawn prompt 模板的每个字段是否已填充？"。这导致 fix-agent 收到不完整的 prompt → fix-agent 调用 fix-loop 时也缺少 --suite/--testcases（见 #15）。 | 在 fix-conductor SKILL.md 阶段 2 硬门检查点（line 112-129）之后，阶段 3 spawn 之前，增加一个 **spawn prompt 完整性检查硬门**：列出 spawn prompt 模板中的所有 `${...}` 占位符，逐项确认已从 RED report 提取并填充。特别检查 `## 目标 testsuite` 和 `## 目标 testcase` 非空。 | plugin-improve references/harness-methodology.md — 护栏机制 #9 "Spawn Prompt Validation": spawn 前必须逐字段验证模板占位符已填充。 |
| 14 | fix-agent 启动初始化: 打印初始化摘要 | ⚠️ | **模板字段不完整。** fix-agent.md:69-90 规定了初始化摘要格式，包含 `screenshot 命令:` 字段（line 89）。实际输出中缺少此字段。agent.md 没有对该字段缺失的硬门检查——缺少一个字段不影响 agent 继续执行，但会丢失可追溯性。 | 在 fix-agent.md 初始化摘要格式后增加硬门检查：**初始化摘要输出后，必须逐字段自检**——对每个 `{从 config.md 解析}` 字段，确认摘要中已输出。发现缺失则补充。或者更简单的方案：从初始化摘要模板中移除 `screenshot 命令:` 字段（因为该命令在 screenshot.md 中定义，不在 config.md 中），避免字段来源不一致。 | fix-agent.md line 89 自身——`screenshot 命令` 字段声明了但 config.md 中没有对应的 `{screenshot_cmd}` 配置项，字段来源不明确。 |
| 15 | fix-agent 启动初始化: 调用fix-loop | ❌ | **上游数据缺失的连锁影响（#11 的直接后果）。** fix-agent.md:96-98 的 fix-loop 调用模板包含 `--suite {suite名}` 和 `--testcases {testcase名列表}` 参数。但从 fix-conductor 收到的 spawn prompt 中无 testsuite/testcase 信息 → fix-agent 调用 fix-loop 时无法填充这两个参数 → fix-loop 在不知道精确测试目标的情况下运行。 | 同 #11 的解决方案。修复 #11（conductor spawn prompt 完整性）后，fix-agent 自然能正确填充 --suite/--testcases。额外措施：fix-agent.md 的 fix-loop 调用处增加**参数非空断言**："如果 suite 或 testcases 为空，停止并报告 conductor——缺少测试目标无法进行修复验证。" | fix-agent.md:96-98 + fix-conductor SKILL.md:267-271 |
| 18 | debug-root-cause 步骤1: 确认BUG存在—跑测试 | ❌ | **护栏缺失：步骤1 没有硬门阻止跳步。** debug-root-cause SKILL.md:33-43 说"跑 BUG 复现测试，捕获确切的失败输出"，但此指令是描述性的（"跑测试"），没有硬门机制（如"未捕获测试输出前不得进入步骤2"）。Red Flags 列表（line 160）中有"还没跑测试就开始写 debug-analysis.md"作为红旗，但 agent 在上下文中需要主动自我检查——没有机制强制它停下来。**深层原因：** fix-loop 调用 debug-root-cause 时未传入 testsuite/testcase（见 #15），导致 debug-root-cause 不知道要跑什么测试，只能跳过步骤1 直接读代码。 | 1) 修复 #11/#15 确保 testsuite/testcase 传入 debug-root-cause。2) 在 debug-root-cause SKILL.md 步骤 1 末尾增加硬门：**"步骤 1 硬门：GUT 测试已执行，失败输出已捕获并记录在下方。未记录测试输出 = 步骤 1 未完成，不得进入步骤 2。"** 3) 在 debug-root-cause 的输入表中增加 `test_runner_cmd` 字段，使 skill 能自行构造测试命令而非依赖外部传入。 | debug-root-cause SKILL.md:33-43 + line 160 Red Flag。plugin-improve references/harness-methodology.md — 护栏机制 #1 "Hard Gate Before Next Step"。 |
| 20 | debug-root-cause 步骤4: 最小验证 | ❌ | **与 #18 同根因：护栏缺失。** SKILL.md:102-113 描述了最小验证流程（修改→测试→撤销），但同样缺少硬门。agent 形成了根因假设后直接写入 debug-analysis.md，跳过了验证步骤。debug-analysis.md "证据链"§3 的标题是"最小验证结果"但内容是"代码 reading 确认"——agent 知道有这个章节要求但用代码分析替代了实际验证。 | 在 debug-root-cause SKILL.md 步骤 4 末尾增加硬门：**"步骤 4 硬门：debug-analysis.md 的 '证据链' §3 必须包含：临时修改了什么（具体行号/内容）、GUT 测试结果（FAIL → PASS）、已撤销确认（git checkout 或手动还原）。不含以上三项 = 步骤 4 未完成。"** 同时在步骤 5（写入 debug-analysis.md）之前增加检查：确认步骤 4 的验证修改已撤销，否则 git status 会显示 dirty。 | debug-root-cause SKILL.md:187 "步骤 4 的临时修改必须撤销" + harness-methodology.md — 护栏机制 #1。 |
| 26 | fix-loop Step 6: screenshot 验证 | ⚠️ | **不在范围 — 环境限制。** fix-agent 正确检测到 xvfb 不可用并报告。headless 模式下 Godot 无法截图是 framework 限制。fix-agent 在 fix-summary.md 中如实记录了此限制。**不进入根因分析。** | ⏭️ 不在范围 | — |
| 30 | VERIFY screenshot 验证 | ⚠️ | **不在范围 — 3 个 grenade 截图失败根因是 player 加载问题（目标项目代码）。** 但 2 个成功的截图证明了截图在无 xvfb 环境下可行，修正了 #26 的"环境限制"误判。**不进入根因分析。** | ⏭️ 不在范围 | — |
| 23a | fix-loop Step 5: 武器缩略图为纯色方块而非真实图标 | ❌ | **Workflow 设计缺口：fix 工作流缺少资产生成阶段。** fix-conductor 的工作流是 `行为澄清 → test agent → fix-agent (fix-loop + debug-root-cause) → VERIFY`。该链路完全覆盖代码 BUG（逻辑错误、数据流问题），但不覆盖视觉资产缺失。当 BUG 是"缩略图不显示"时，代码修复只能让 slot 从"什么都不显示"变成"显示程序化占位色块"，而无法产出真正的武器 3D 模型渲染图标。插件有 `references/godot/asset-gen.md` 定义了 mmx image generate 能力，但 fix 工作流从不触发它。**机制层：** fix-conductor 没有"此 BUG 是否需要资产生成"的检测步骤。 | 在 fix-conductor 阶段 1（行为澄清）之后、阶段 2（spawn test-agent）之前，增加 **资产需求检测**：扫描 BUG 描述和预期行为中的视觉资产关键词（缩略图/图标/贴图/纹理/sprite/texture/thumbnail/icon），命中后执行：1) 在 requirements.md 中标注 `[ASSET-REQUIRED]` 标签 2) 在 fix 工作流中插入 asset generation 步骤——调用 game-dev:art-resource-creator agent 或直接 prompt fix-agent 使用 asset-gen.md + mmx 生成资产 3) 生成的资产路径写入 requirements.md 供 fix-agent 引用 | fix-conductor SKILL.md 工作流总览（缺少资产生成阶段）+ references/godot/asset-gen.md（存在但 fix 链路不使用）。plugin-improve references/harness-methodology.md 护栏机制 #3 "Missing Phase Detection"。 |
| 24a | fix-agent 测试执行方法—log_path 重定向 | ❌ | **缺少 Hard Gate 强制兑现声明的执行方法。** fix-agent.md:57 声明 `> {log_path}`，但声明后没有机制确保 agent 执行时替换 `{log_path}` 并保存输出。agent 把声明当模板格式而非可检查的承诺。对照 harness-methodology.md §4.1："症状: 模型跳过了流程步骤 → 可能缺失: Hard Gate (机制5) → 修复: 在该步骤后加入 Hard Gate"。 | 在测试执行方法声明后增加 Hard Gate：**"每次测试执行后：确认 `{log_path}` 已被替换为实际文件路径，测试输出已 `>` 重定向到该文件。管道可用于二次提取但原始输出必须先完整保存。日志文件不存在 = 测试验证无效。"** | harness-methodology.md 机制 5 (Hard Gate) + fix-agent.md:57（已声明的 `> {log_path}` 方法） |
| 26 | fix-loop Step 6: screenshot 验证 | ❌ | **渐进式披露违规。** screenshot.md 在启动时与 coding.md 等批量预载并标记 ✅（L29），但到执行截图时（L362, L365）已过几十步，约束（"绝不能加 --headless"）已不在工作记忆。agent 把 "已读 ✅" 当成完成而非承诺。对照 harness-methodology.md：reference 文件"如果在 agent 需要使用它的时候没有被重新加载，agent 不会记住其中的约束"。 | 1) screenshot.md 从启动批量读取列表移除 2) 执行截图前 Hard Gate：先 `Read ${CLAUDE_PLUGIN_ROOT}/references/{tech}/screenshot.md`，提取 CLI 命令模板和所有约束，逐条确认即将执行的命令符合约束。读完立即执行，不间隔其他操作。3) 同理适用于 3d-construction.md（需要构造 3D 资源时再读）。 | harness-methodology.md §3.3 Reference 文件的 Harness 要素 + 渐进式披露原则 + fix-agent.md 启动初始化 step 3 |
| 31 | test-agent RED Step 0: 读取design.md | ❌ | **任务类型假设错误。** test-agent RED mode Step 0（SKILL.md:86-91）无条件列出两个文件 requirements.md + design.md，但 design.md 只在 feat/refactor 工作流中由 plan 阶段产出。fix 工作流不经过 plan 阶段，只有 requirements.md。test-agent 在 fix 工作流中每次都会遇到 design.md 不存在的错误。虽然 agent 正确降级处理了（跳过继续），但：1) 浪费了一次 Read 调用；2) Step 0 的声明与实际 fix 工作流不匹配，降低了 agent 指令的可信度。 | 修改 test-agent RED mode Step 0 的文件列表：将 `{task_dir}/.work/design.md` 改为 `{task_dir}/.work/design.md（如存在）`。或更干净：从 Step 0 移除 design.md，在 Step 1 "从行为清单推导 testcase" 中增加说明"如有 design.md，参考其中的信号名、节点路径"。fix 工作流不需要 design.md 是因为 requirements.md 已包含足够的公共接口信息。 | test-agent.md:86-91 + fix-conductor SKILL.md 工作流总览（requirements.md 是 fix 工作流唯一的设计文档输入）。 |
| 32 | VERIFY test-agent 也读取design.md | ❌ | **同 #31 的根因。** VERIFY test-agent 也尝试读取 design.md。但 GREEN mode 的 SKILL.md 并没有显式声明读 design.md——这是 agent 的自主行为，可能源于 test-agent 的 Startup 初始化流程（line 36-39）没有按 mode 区分文件读取列表。 | 同 #31 的解决方案。额外措施：test-agent 的 Startup 章节明确区分 RED mode 和 GREEN mode 的文件读取列表。GREEN mode 不需要读设计文档——它只需要 config.md + testing.md + screenshot.md + requirements.md（用于了解 screenshot 验证需求）。 | test-agent.md:36-39 (Startup) + test-agent.md:317-327 (GREEN mode) |
| 33 | debug-root-cause 步骤1: Screenshot测试确认 | ❌ | **同 #18 的根因。** debug-root-cause 完全跳过了步骤 1，因此 screenshot 测试也未执行。此外 screenshot 测试执行需要非 headless 环境——在当前 session 中不可用。但 skill 没有区分"跳过"和"尝试后失败"——步骤 1 的 screenshot 部分至少应尝试执行并记录失败原因。 | 同 #18 的解决方案——先修复步骤 1 的硬门。对于 screenshot 测试的环境限制，在步骤 1 的硬门中增加：**"如有 screenshot 测试但环境不支持（无 xvfb），记录环境限制到 debug-analysis.md 的证据链§1，明确标注 'screenshot 验证跳过: 环境不支持'"，不算步骤 1 不完整。** | debug-root-cause SKILL.md:44 + harness-methodology.md 护栏机制 #1。 |
