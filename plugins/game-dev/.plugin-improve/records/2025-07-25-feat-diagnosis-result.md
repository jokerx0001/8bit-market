# game-dev - feat 链路诊断结果
## 日期：2025-07-25

### 链路拓扑

```
skills/orchestrator/SKILL.md (入口)
├── Skill: game-dev:artifact-manager → skills/artifact-manager/SKILL.md
├── Skill: game-dev:requirements → skills/requirements/SKILL.md
├── Skill: game-dev:concept-art → skills/concept-art/SKILL.md
├── Skill: game-dev:design-ui → skills/design-ui/SKILL.md
├── Skill: game-dev:plan → skills/plan/SKILL.md
├── Skill: game-dev:asset-extract-doc → skills/asset-extract-doc/SKILL.md
├── Skill: game-dev:art-resources-conductor → skills/art-resources-conductor/SKILL.md
├── Skill: game-dev:exec ⭐ (screenshot 关键路径)
│   └── skills/exec/SKILL.md
│   ├── ref: skills/exec/references/exec-prompts.md
│   ├── ref: skills/exec/references/exec-logging.md
│   ├── ref: references/plan-format.md
│   ├── ref: references/godot/config.md
│   ├── Agent: game-dev:test-agent → agents/test-agent.md
│   │   ├── ref: references/godot/testing.md
│   │   └── ref: references/godot/screenshot.md
│   └── Agent: game-dev:coding → agents/coding.md
│       └── ref: references/godot/screenshot.md
├── Skill: game-dev:ui-restoration → skills/ui-restoration/SKILL.md
└── Skill: game-dev:architecture → skills/architecture/SKILL.md
```

### 逐步骤诊断

**诊断范围说明：** 用户报告 "screenshot 一个都没有"。plan.md 行为列表中 19 条行为中有 13 处 `screenshot:` 验证方式。产物目录 `/Users/jokerx/project/godot/shadow-punk/.godot-dev/feat-2/` 中 `.work/screenshots/` 目录不存在，零张截图 PNG。test/visual/ 下有 42 个截图脚本和 .question 文件（test-agent 已创建），但从未被执行。

| # | 所属节点 | 应有步骤 | 要求(来自plugin文件) | 实际步骤(来自log) | 达标? | 达标证据 / 不达标点 |
|---|---------|---------|---------------------|------------------|-------|-------------------|
| 1 | skills/exec/SKILL.md L103-111 | 步骤3: 解析行为列表，提取 screenshot 验证需求 | "从 plan.md 的 `## 行为列表` 表格中提取每条行为的验证方式：`grep 'screenshot' {task_dir}/plan.md`。有 screenshot 行为 → 记录 screenshot 验证需求列表（行为 # + 问题描述），在 RED spawn prompt 中传入。" | plan.md 有 13 处 "screenshot"（经 `grep -c screenshot plan.md` 确认 = 13）。requirements.md 有 14/19 行为标记 `screenshot:`。 | ❌ | **数据传递断裂：** exec-prompts.md RED 模板（L16-41）无专用 `## 目标 screenshot testcase` 字段。RED 模板仅含 `## 要验证的行为`（来自 plan.md 测试策略表），不含截图验证需求列表。test-agent 虽能通过读 requirements.md 自行发现截图行为（事实上 test-agent for AI-4 确实创建了 6 个截图脚本），但 exec 声明"在 RED spawn prompt 中传入"的任务并未执行——RED 模板没有对应的字段可以承载这些数据。 |
| 2 | skills/exec/references/exec-prompts.md L16-41 | RED 模板: 组装 test-agent spawn prompt | RED 模板字段：`## 模式`、`## project`、`## task_dir`、`## 任务`、`## 要验证的行为`、`## 需要读取的文件`。**无 screenshot 专用字段。** | RED 模板中缺少 `## 目标 screenshot testcase` 字段。 | ⚠️ | **风险：模板字段缺失。** RED 模板声明 "在 RED spawn prompt 中传入" screenshot 验证需求（来自 exec SKILL.md L103-111），但模板本身没有对应字段。test-agent 通过自行读取 requirements.md 弥补了这个缺口（AI-4 确实创建了 6 个截图脚本），但这意味着 screenshot 数据传递依赖 test-agent 的"自行发现"而非 exec 的"显式传入"，不可靠。 |
| 3 | agents/test-agent.md L107-125 | RED Step 1: screenshot标识行为必须有截图testcase | "screenshot标识的行为,需要额外要求。**每条** screenshot 行为独立产出**一对**文件（截图脚本 + .question），不可将多条行为合并到一个截图脚本中。screenshot标识行为必须有截图testcase,否则编写无效。" | test/visual/ 下有 42 个文件（.gd + .question 配对），证明 test-agent 为 AI-4 创建了截图脚本。tdd-iterations.md AI-4 节记载 "6 screenshot scripts created"。 | ✅ | tdd-iterations.md L148: "6 screenshot scripts created"。test/visual/ 目录下存在 test_cyber_tang_hero_attack.gd + .question、test_cyber_tang_hero_air_attack.gd + .question、test_cyber_tang_hero_combo_two.gd + .question、test_cyber_tang_hero_combo_three.gd + .question、test_cyber_tang_hero_hud.gd + .question、test_cyber_tang_hero_weapon_cycle.gd + .question 等配对文件。 |
| 4 | agents/test-agent.md L173-183 | Screenshot Iron Law: 截图 = 实际游戏场景 viewport 截图 | "SCREENSHOT MEANS VIEWPORT CAPTURE OF THE ACTUAL GAMEPLAY SCENE. NOT PROGRAMMATIC DRAWING. A screenshot script that does not call get_viewport().get_texture() is not a screenshot script." | test_cyber_tang_hero_attack.gd (L40) 使用 `get_root().get_viewport().get_texture().get_image()` — 符合 Iron Law。但使用的是 `hero_scene.instantiate()` + `get_root().add_child(hero)`（L19-20）而非 `change_scene_to_file` 加载关卡场景。 | ⚠️ | **风险：场景加载方式不符合 screenshot.md 场景选择规则。** 脚本加载独立 .tscn 组件 (`cyber_tang_hero.tscn`) 而非关卡场景。screenshot.md 场景选择规则（test-agent.md L209-217）明确："角色外观/动画 → 加载关卡场景（如 level.tscn），角色通过 Global 或关卡逻辑自然加载"。"只加载 `tang_guard.tscn` 独立组件"明确列为错误做法。然而这些脚本从未被执行，无法确认它们在真实关卡中是否能正常工作。 |
| 5 | agents/test-agent.md L291-308 | RED Step 3: 截图脚本执行确认 | "RED 阶段执行一次截图确认脚本可运行（不调 visual-qa）。确认退出码 0 且输出文件非空。" | `.work/screenshots/` 目录不存在，零张 PNG。coding 日志中无 screenshot 执行记录。 | ❌ | **步骤未执行。** Screenshot 脚本存在于 test/visual/ 但从未被执行过。RED 阶段应执行一次截图确认脚本可运行，但没有任何日志或产物证明此步骤发生。test-agent Critical Rule #8（L400-402）声明 "RED 阶段执行一次截图确认脚本可运行（不调 visual-qa）"——这条规则未被遵守。 |
| 6 | skills/exec/references/exec-prompts.md L51-91 + agents/test-agent.md L361-364 | GREEN 模板: 传递 screenshot testcase 到 coding-agent | "`## 目标 screenshot testcase（如有）`— 从 test-agent RED report 的 `### Screenshot Testcases` 表格提取。" GREEN 需要：testcase_name + 脚本路径 + question 路径。 | test-agent RED report 格式（L361-364）有 `### Screenshot Testcases` 段，但列定义为 `\| Testcase \| 问题 \| 产出文件 \|`。"产出文件"列写的是自然语言 ".gd + .question 已创建" 而非实际文件路径。GREEN 模板需要脚本路径和 question 路径做结构化提取，但原表格式无法被消费。 | ❌ | **RED 报告格式不可提取（关键断裂点）。** test-agent 确实产出了 Screenshot Testcases 段，但列数据不可被下游消费——"产出文件"是自然语言而非文件路径。GREEN 模板的提取指令（"从表格提取 testcase_name、脚本路径、question 路径"）面对 "产出文件: .gd + .question 已创建" 这种值无法工作。同时 exec-logging.md RED 模板也未要求记录 screenshot 详细信息到 tdd-iterations.md。exec 在两个地方都无法获取结构化数据 → GREEN spawn prompt 的 screenshot testcase 为空 → coding-agent 从未执行截图验证。 |
| 7 | agents/coding.md L69, L79-83, L170 | GREEN Phase 1: screenshot testcase 同步执行 | "如有 screenshot testcase（spawn prompt 中 `## 目标 screenshot testcase` 非空且不为 '无'）：在 Phase 1 必须同步执行——对每个 screenshot testcase 运行截图脚本 → visual-qa → 结果写入 `screenshot_<name>_run<N>.log`" | `.work/coding/` 目录中无任何 `screenshot_*` 日志文件。无 visual-qa 执行痕迹。 | ❌ | **步骤未执行。** Coding agent 从未收到 screenshot testcase（见 #6），因此从未执行 Phase 1 的截图+visual-qa。`.work/coding/` 下无 screenshot 相关日志文件。init.log 中 coding-agent 记录了正确的截图执行方法（L101-107：godot --path → base64 -d → Skill visual-qa），但从未被调用。 |
| 8 | skills/exec/SKILL.md L220-226 | 步骤 6c: GREEN 检查 — screenshot visual-qa PASS | "- [ ] 有 screenshot 验证方式的行为：visual-qa PASS" | tdd-iterations.md 中 AI-4 的 GREEN 判定（L198）为 "✅ 实现正确（输入依赖测试受限于GUT headless）"。 | ❌ | **检查项被跳过。** Exec 在 GREEN 检查清单（L220-226）中声明要检查 "有 screenshot 验证方式的行为：visual-qa PASS"，但 AI-4 的 GREEN 判定直接标记 ✅，没有任何 visual-qa 结果被检查。Exec 未执行其声明的 screenshot 检查——因为根本没有 visual-qa 结果可供检查。 |
| 9 | skills/exec/SKILL.md L242-245 | 步骤 6d: VERIFY 检查 — 截图文件有效性 | "- [ ] 有 screenshot 验证方式的行为：所有截图 testcase visual-qa PASS（**零失败容忍**）" + "- [ ] 有 screenshot 验证方式的行为：所有截图文件为有效 PNG（`file {path}` 输出含 'PNG image data'）。非图片文件 → 截图脚本执行失败" | tdd-iterations.md AI-4 VERIFY (L203): "全量: 8/15 CYBERTANG only。Verdict: ✅ 非输入依赖全部通过" | ❌ | **检查项被跳过。** Exec 声明要检查 screenshot testcase visual-qa PASS 和截图文件有效性，但 AI-4 VERIFY 判定直接标记 ✅，screenhot 检查完全被跳过。`.work/screenshots/` 目录不存在——没有任何截图文件供检查。 |
| 10 | skills/exec/SKILL.md L372 | Completion Gate #4: screenshot 验证完成 | "所有 screenshot 验证行为已创建截图 testcase 且通过 visual-qa" | progress.json: 所有 AI 任务 status = "done"。任务被标记为完成。 | ❌ | **Completion Gate 未满足但任务仍标记完成。** 截图 testcase 虽已创建（test/visual/ 下的脚本和 .question 文件），但从未通过 visual-qa（从未被执行）。Completion Gate #4 要求 "所有 screenshot 验证行为已通过 visual-qa"——此条件不满足，但 progress.json 仍将所有任务标记为 done。 |
| 11 | agents/coding.md L739 | Rule #14: screenshot 自验证 | "screenshot 验证方式自验证：截图 + visual-qa。自验证时使用 spawn 初始化中解析的测试执行方法（截图脚本 → `Skill("game-dev:visual-qa")`），与其他 testcase 同等对待——逐个击破、根因分析、先记日志再改代码。" | 无执行痕迹。 | ❌ | **步骤未执行。** Coding agent 的 Rule #14 强调 screenshot testcase 与 GUT testcase "同等对待"，但对 AI-4 的 coding agent spawn，screenshot testcase 完全未被处理。根因是 #6——coding agent 从未收到 screenshot testcase 列表。 |

### 根因分析与解决方案

| # | 应有步骤 | 达标? | 根因 | 解决方案 | 解决方案来源 |
|---|---------|-------|------|---------|-------------|
| 1 | 步骤3: 解析行为列表，提取 screenshot 验证需求 | ❌ | **Spawn prompt 模板字段缺失。** exec SKILL.md L103-111 声明 "在 RED spawn prompt 中传入" screenshot 验证需求，但 exec-prompts.md RED 模板没有对应的 `## 目标 screenshot testcase` 字段。数据流 exec→RED 的传递接口不完整。这是 spawn prompt 模板设计缺陷——模板定义了 exec 传什么给 agent，但 screenshot 数据没有对应的承载字段。属于 diagnosis-guide.md §2.4 "节点之间的数据传递不一致" → Harness 机制5 (Hard Gate) 在传递边界缺失。 | **在 exec-prompts.md RED 模板中新增 `## 目标 screenshot 行为` 字段。** 在 `## 要验证的行为` 之后增加：`## 目标 screenshot 行为（如有）` 字段，填入从 plan.md 行为列表 grep screenshot 得到的行为 # + 验证问题描述。格式：`- 行为 #N: {验证问题描述}`。同时 exec SKILL.md 步骤 6b RED 检查清单增加一项 "screenshot 行为已传入 RED spawn prompt"。 | harness-methodology.md §机制5 (Hard Gate): "一个显式的 DO NOT proceed until X 声明，放在两个阶段/步骤之间"；§机制6 (Phase Transitions): "每个阶段有明确的入口条件" |
| 2 | RED 模板: 组装 test-agent spawn prompt | ⚠️ | **模板字段缺失（同 #1）。** RED 模板缺少 screenshot 字段，导致 screenshot 数据传递依赖 test-agent 自行读取 requirements.md 的"偶然正确"行为，而非 exec 的显式传入。这不是确认的 bug（log 中无文件未找到错误），但存在结构性风险：如果 requirements.md 的截图行为描述不完整或格式变化，test-agent 可能漏掉截图行为。 | **同 #1。** 在 exec-prompts.md RED 模板中新增 screenshot 字段。额外的加固：exec SKILL.md 步骤 3 的 grep 结果写入 task_dir 临时文件（如 `.work/screenshot-behaviors.txt`），RED spawn prompt 引用此文件路径而非内联全部内容（防止 prompt 过长）。 | harness-methodology.md §机制5; skill-structure.md §4.4 "详细内容移到 references/ 而非全塞在 SKILL.md" |
| 3 | RED Step 1: screenshot标识行为必须有截图testcase | ✅ | N/A | N/A | N/A |
| 4 | Screenshot Iron Law: 截图 = 实际游戏场景 viewport 截图 | ⚠️ | **场景加载规则执行不一致。** test-agent.md 的场景选择规则表（L209-217）明确了每种行为类型应加载的场景，Red Flags（L188-197）列举了禁止的做法（"加载独立组件 .tscn"）。但创建的截图脚本使用了 `hero_scene.instantiate()` + `add_child()` 而非 `change_scene_to_file` 加载关卡。脚本从未被执行，无法确认此风险是否会导致实际问题。log 中无文件未找到等证据。 | **test-agent.md RED Step W2 场景选择规则表增加自检步骤。** 在截图脚本编写完成后、RED 报告前，增加：对照场景选择规则表逐条自查加载的场景是否正确。自查项：① 加载的场景是否匹配规则表中的"行为类型 → 加载的场景"映射？② 是否使用了规则表"错误做法"列中的任何模式？发现违规则重写脚本。 | harness-methodology.md §机制8 (Self-Review Checkpoint): "在声明'完成'之前，强制自己跑一遍检查清单" |
| 5 | RED Step 3: 截图脚本执行确认 | ❌ | **步骤无强制门控。** test-agent Critical Rule #8 声明 "RED 阶段执行一次截图确认脚本可运行"，但这条规则不是 Iron Law——没有 ALL CAPS 声明、没有 Hard Gate、不执行没有后果。agent 在 RED 模式下创建了很多 GUT testcase + 截图脚本后，可能因上下文耗尽或优先级判断而跳过了截图执行确认。属于 diagnosis-guide.md §2.1 "模型跳过了某个流程步骤" → Harness 机制1 (Iron Law) 缺失。 | **将 "RED 阶段确认截图脚本可运行" 升级为 Iron Law + Hard Gate。** 在 test-agent.md Screenshot 测试编写方法章节开头增加 Iron Law：`SCREENSHOT SCRIPTS MUST BE EXECUTED AND CONFIRMED RUNNABLE IN RED PHASE. 未执行确认的截图脚本 = RED 不合格。` 在 RED Step 3 末尾增加 Hard Gate：`**BEFORE reporting RED: 每个截图脚本已执行一次，退出码 0 且输出 PNG 非空。未执行 → RED 不合格，重做 Step 3。**` | harness-methodology.md §机制1 (Iron Law): "一条 ALL CAPS 的、不可谈判的规则，放在最前面"；§机制5 (Hard Gate): "DO NOT proceed until X" |
| 6 | GREEN 模板: 传递 screenshot testcase 到 coding-agent | ❌ | **数据传递断裂（链路级根因）。** 断裂发生在两个环节：① RED 报告提取 — exec 应从 test-agent RED report 的 `### Screenshot Testcases` 表中提取 testcase 名称、behavior 描述、脚本路径、question 路径，填充到 GREEN 模板的 `## 目标 screenshot testcase` 字段。但 tdd-iterations.md 中 AI-4 RED 报告未记录 Screenshot Testcases 表（只写了 "6 screenshot scripts created"），exec 无法从迭代日志中提取。② coding-agent 的 init.log 追加失败 — 只有 AI-1 的条目，后续 spawn 未追加，导致无证据确认 screenshot testcase 是否被传入。属于 diagnosis-guide.md §2.4 → Harness 机制5 (Hard Gate) 在 spawn 边界缺失。 | **三处修复：① exec-logging.md RED 模板增加 Screenshot Testcases 表的日志格式**（脚本路径 + question 路径 + behavior 描述）；**② exec-prompts.md GREEN 模板的 `## 目标 screenshot testcase` 字段改为从 exec 在第 3 步记录的 `.work/screenshot-behaviors.txt` + RED report 共同填充**，而非仅依赖 RED report 格式；**③ exec SKILL.md 步骤 6c GREEN 检查增加 Hard Gate：screenshot 行为 > 0 但 GREEN prompt 中 `## 目标 screenshot testcase` 为 "无" → 禁止 spawn，先补全数据。** | harness-methodology.md §机制5 (Hard Gate): "spawn prompt 模板包含所有必要上下文"；harness-methodology.md §机制6 (Phase Transitions): "入口条件验证" |
| 7 | GREEN Phase 1: screenshot testcase 同步执行 | ❌ | **下游影响（根因同 #6）。** Coding agent 未收到 screenshot testcase，因此 Phase 1 的截图+visual-qa 从未触发。这是 #6 的直接后果，不是独立根因。 | **同 #6。** 修复 #6 后，此步骤自动修复。额外的保险：coding.md Phase 1 增加自检 — 若 `## 目标 screenshot testcase` 非空但 Phase 1 未执行截图 → 标记为流程违规，禁止进入 Phase 2。 | 同 #6 |
| 8 | 步骤 6c: GREEN 检查 — screenshot visual-qa PASS | ❌ | **检查清单无后果声明。** exec SKILL.md L220-226 的 GREEN 检查清单列出了 "有 screenshot 验证方式的行为：visual-qa PASS"，但没有 Hard Gate —— 不满足时只是 "指出具体问题，重新 spawn"，没有 "未通过则 GREEN 不合格" 的强制后果。当 screenshot 数据不存在时，exec 跳过了此项检查直接标记 ✅。属于 diagnosis-guide.md §2.2 "模型声称完成但产物不符合描述" → Harness 机制13 (Checklist with Consequences) 缺失。 | **将 GREEN 检查清单升级为 Checklist with Consequences。** 在 exec SKILL.md L220-226 的检查清单后增加：`**0 项未打勾 → GREEN 通过。任一项未打勾 → GREEN 不合格，重新 spawn。screenshot 行为 > 0 但 visual-qa PASS 项未打勾 → 禁止进入 VERIFY。**` | harness-methodology.md §机制13 (Checklist with Consequences): "Can't check all boxes? [Consequence statement]" |
| 9 | 步骤 6d: VERIFY 检查 — 截图文件有效性 | ❌ | **检查清单无后果声明（同 #8）。** VERIFY 检查清单（L242-245）同样缺少 Hard Gate 后果声明。screenshot 检查项被跳过而不触发任何阻止机制。 | **同 #8。将 VERIFY 检查清单升级为 Checklist with Consequences。** 增加：`**screenshot 行为 > 0 但 `file {path}` 不是 "PNG image data" → VERIFY 不合格，回退 GREEN 重做。**`| harness-methodology.md §机制13 |
| 10 | Completion Gate #4: screenshot 验证完成 | ❌ | **Completion Gate 无强制验证机制。** exec SKILL.md L368-376 列出了 8 条 Completion Gate，但没有逐条验证的 Hard Gate 流程。exec 在任务末尾应逐条检查 Completion Gate 并确认全部满足才标记 done，但实际行为是直接标记 done 而不验证。属于 diagnosis-guide.md §2.2 → Harness 机制8 (Self-Review Checkpoint) + 机制13 (Checklist with Consequences) 缺失。 | **在 exec SKILL.md 步骤 6h "标记完成" 之前增加 Completion Gate 逐条验证流程。** 格式：`**BEFORE updating progress.json to done:** 逐条检查以下 Completion Gate 条件。全部 ✅ 才可标记 done。任一条 ❌ → 禁止标记 done，回到对应阶段修复。` 每条 Gate 是硬检查点。特别地，Gate #4（screenshot）：`grep -c 'screenshot' plan.md > 0 但 .work/screenshots/ 无 PNG 文件 → ❌，禁止标记完成。` | harness-methodology.md §机制8 (Self-Review Checkpoint): "在声明'完成'之前，强制自己跑一遍检查清单"；§机制13 (Checklist with Consequences): "带后果的检查清单" |
| 11 | Rule #14: screenshot 自验证 | ❌ | **下游影响（根因同 #6）。** Coding agent 未收到 screenshot testcase，因此 Rule #14 的自验证协议从未被触发。这是 #6 的直接后果。 | **同 #6。** 修复 #6 后此步骤自动修复。 | 同 #6 |
