---
name: refactor
description: "启动游戏项目重构工作流。自动检测技术栈，分析影响范围 → impact.md → plan → exec --mode refactor。"
argument-hint: "<重构目标> [约束条件] [--auto]"
---

Invoke the `game-dev:refactor-conductor` skill with the user's arguments. Pass `--auto` for fully autonomous mode.
