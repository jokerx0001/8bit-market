using UnityEngine;

namespace Game.Data
{
    [CreateAssetMenu(fileName = "{Name}Data", menuName = "Game/{Name}Data")]
    public class {Name}Data : ScriptableObject
    {
        // Define data fields only — NO logic, NO methods
    }
}
