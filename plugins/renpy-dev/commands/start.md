---
name: start
description: "启动 Ren'Py 新功能开发工作流。使用 orchestrator 协调 plan → exec → review 全流程。"
argument-hint: "<任务描述> [--auto]"
---

Invoke the `renpy-dev:orchestrator` skill with the user's arguments. Pass `--auto` if the user wants fully autonomous mode without waiting for human review checkpoints.
